# 회고 반영 대기열

retro 스킬이 낸 후보 중 **사용자가 승인했으나 아직 반영하지 않은 것**만 여기 있다. 후보와 기각은
일지에 남고 이 파일에 오지 않는다. 반영한 줄은 취소선을 긋고 어디서 닫혔는지 적으며, 지우는 것은
다음 회고다. 바로 지우지 않는 이유는 지시문이 들고 다니는 수와 대조할 수 있게 하기 위해서다.

이 파일이 생긴 이유는 대기열이 일지 산문에만 있어 매번 열여섯 개 파일을 세야 했기 때문이다
(일지 `2026-09-22-10`의 승인 2).

기본 반영 자리는 `chore/<slug>` 브랜치다. 계약이나 헌법에 닿는 것은 ADR과 승인이 따로 필요해
"어디로"에 적는다.

| # | 무엇 | 어디로 | 승인 |
|---|---|---|---|
| 1 | `operations.md`에 `-m llm`을 돌리는 계기와, 일지 "검사" 절에 트레이스의 토큰 합계를 적는 한 줄. **계기의 문구가 나왔다 — 테스트를 건드렸을 때가 아니라 그 테스트가 지나는 코드를 건드렸을 때.** PR #46에서 LLM 테스트를 한 줄도 안 고치고 그 아래 배관(`run_command` → `_report` → `_show` → `_approval_request`) 시그니처를 전부 바꿔 돌렸다. 템플릿 문언 "건드렸다면"은 문자 그대로는 면제였다 | `docs/constitution/operations.md` + `.github/PULL_REQUEST_TEMPLATE.md` | 일지 06·07, 문구는 PR #46의 회고 |
| 2 | "일지는 code-review 뒤에 쓴다"를 `implement`에만 두지 않고 `next-session`으로 옮기거나 양쪽에 둔다. PR을 내는 세션이 `implement`만이 아니다 | `.claude/skills/next-session/` | 일지 05·08 |
| 3 | 넘겨받은 Out of Scope를 찾는 한 줄. 미루는 쪽은 적지만 받는 쪽이 읽는다는 보장이 없다. **2회차로 넓혔다 — 리뷰·일지의 "그 층에서 재확인이 필요하다"도 그 층을 소유한 티켓의 수용 기준으로 간다.** 01 리뷰가 표지 reason을 "오탐, 단 HTTP 층에서 재확인"으로 두었는데 05 티켓에 오지 않아 PR #56의 봇이 Critical로 잡았다. 같은 01이 커서 한계는 05 수용 기준에 넣어 제대로 닫혔다. **3회차로 넓혔다 — 받는 쪽에 적어도 받는 쪽이 먼저 돌 수 있다.** http-channel 티켓 세션이 3.1·3.2 측정을 받는 쪽인 `plan.md`의 web-admin 행에 제대로 적었는데, web-admin은 admin-api에만 막혀 이미 프론티어였다. 채널 라우트가 붙기 전에 생성기를 고르면 SSE가 없는 파일로 재 아무것도 재지 않고 통과한다(명세 축이 잡았다). 한 구절: "넘길 때 받는 쪽의 Blocked by가 넘기는 쪽을 포함하는지 본다" | `to-spec`·`to-tickets` 스킬 + `code-review` 6단계(보류를 적는 자리) | 일지 07, 일지 2026-09-23-04, 2026-09-24-05 |
| 4 | `sdk`의 `ApprovalDenied.reason`에 `min_length=1`. **계약 변경이라 ADR 0009 이력과 사용자 승인이 먼저다. 별도 PR** | `src/agent_os/sdk/events.py` + ADR 0009 | 일지 06 |
| 5 | PR 템플릿의 `coderabbit-review` 줄을 날카롭게 — "문서만 바뀐 PR도 면제가 아니다. 설계 문서의 침묵이 곧 구현의 침묵이다"(PR #38 CWE-319, #39 CWE-22, 둘 다 코드 0줄). **3회차**: PR #67(http-channel 명세, 코드 0줄)에서 CodeRabbit이 CWE-94를 냈다. 명세가 "무엇이 원격 입력이 되나"(에이전트 이름)는 적고 "무엇은 여전히 신뢰 경계 안인가"(매니페스트·진입점·MCP 명령)는 적지 않았다. CLI 좌석이 없어 이 축은 PR 봇 하나였다. **4회차**: PR #68(http-channel 티켓, 코드 0줄)에서 PR 봇이 CWE-209를 냈다(채널의 500 봉투가 `PluginError` 원문을 싣는다, 보류). CLI는 티켓의 보편 주장 하나를 잡았다. **좌석은 PR마다 바뀐다** — #67에서 `Seat: not assigned`였던 CLI가 #68에서는 돌았다. 템플릿의 "좌석이 없으면 돌리지 않고"를 직전 PR의 확인만으로 따르면 좌석이 생긴 뒤에도 축 하나를 계속 건너뛴다. 한 구절: "좌석은 PR마다 `coderabbit auth status` 한 줄로 본다. 직전 PR의 확인은 캐시다" | `.github/PULL_REQUEST_TEMPLATE.md` | 일지 08, 2026-09-24-04, 05 |
| 6 | `sdk/ids.py`의 주석이 `NewType`의 구멍을 이점으로만 적는다. "경계에서 오는 값은 부르는 쪽이 검증한다" 한 절을 개념 옆에 | `src/agent_os/sdk/ids.py` | 일지 08 |
| 7 | 기능 하나의 실행 순서가 `KICKOFF.md`에만 있다. 강제력이 높은 자리는 지도 표가 아니라 `to-tickets` 스킬 1단계 | `.claude/skills/to-tickets/` | 일지 09 |
| 8 | ~~명세 검토 프롬프트의 질문 둘(체크리스트 일곱에 없던 것)을 `KICKOFF.md` 9단계 3번에. 다음 검토가 2회째가 되어 파일로 굳을 때 함께 간다~~ | `KICKOFF.md` + `.claude/agents/spec-reviewer.md` | **닫힘 (`docs/http-channel-tickets`, 일지 2026-09-24-05)** |
| 9 | ~~헌법 거버넌스가 "버전은 semver, 버전은 `README.md`에 있다"고 적는데 **README에 버전이 없다.** ADR 0013의 문구 수정이 patch 올림을 요구하는데 올릴 대상이 없어서 발견했다. 시작 값이 무엇인지는 사용자 판단이라 지어내지 않았다~~ 전제가 틀렸다. 버전은 헌법 README에 있었다(2.4.0 문단) | `docs/constitution/README.md` + `principles.md` 서두·거버넌스 | **닫힘 (`chore/constitution-version-drift`, 일지 2026-09-25-01)** |
| 10 | PR 템플릿의 하네스 줄이 "새 검사는 변이로 빨강을 봤는가"만 묻는다. **변이는 내가 생각한 우회로만 잰다** — 검사 하나에 구멍 둘(문자열 주해, `.pyi`)이 있었고 둘 다 변이 목록에 없어 리뷰가 잡았다. 한 구절 더: "이 검사가 **못 보는 것**을 어디에 적었는가". 5번과 같은 파일이라 함께 간다. **2회차로 넓혔다 — 적는 것만으로는 부족하고 그 주장도 잰다.** PR #60의 훅 docstring이 "못 보는 것"에 "사람이 지은 제목이면 앱이 승인을 묻는다"를 적었는데 틀렸고(앱이 지은 제목은 묻지 않고 바뀐다) 바로 그 자리에서 오탐이 났다. 문구: "못 보는 것을 어디에 적었는가, 그 주장을 쟀는가" | `.github/PULL_REQUEST_TEMPLATE.md` | PR #43의 회고, 일지 2026-09-23-06 |
| 11 | 환경 함정의 "파이프 뒤의 `&&`" 줄을 `&&` 체인까지 넓힌다. 검사 일곱을 `&&`로 이었다가 앞이 실패해 뒤가 안 돌았는데 `$?`가 앞 것을 찍어 "지침 검사 실패"로 읽었다. 같은 가족 2회째 **19로 합쳤다. 19가 반영될 때 함께 닫힌다**(일지 2026-09-23-04) | `CLAUDE.md` 환경 함정 | PR #43의 회고 |
| 12 | `tools/`의 식별자 언어 관례를 정한다. `check_instructions.py`·`check_commit_msg.py`는 식별자가 영문이고 문자열·주석만 한국어인데, `check_type_escapes.py`가 지역 변수까지 한국어로 썼다. 테스트 **함수명**의 한국어는 `CODING_STANDARDS.md`가 이미 요구하는 것이라 별개다. 어느 쪽이든 정하고 적어야 다음 사람이 헷갈리지 않는다(PR #43 claude-review Nit) | `CODING_STANDARDS.md` 또는 `.claude/rules/` | PR #43 리뷰 |
| 13 | **커버리지 판정자가 없다.** PR #46의 Major(`serve` 성공 경로 테스트 0개)를 기계가 먼저 가리킬 수 있었다 — 측정으로 확인했다. 성공 경로 테스트 하나를 빼면 334개가 초록인 채로 `main.py:91-101`(`uvicorn.run(create_app(...))`)이 미커버로 뜬다. 방식 셋 중 고르는 것이 남았다: 리포트만 / diff에 새로 생긴 미커버 줄만 실패 / 검증 명령에 추가. **퍼센트 하드 게이트는 거짓 양성이 많아 제외.** 의존성과 검증 명령을 건드리므로 ADR과 승인이 먼저다 | `pyproject.toml` + `tech.md`·`operations.md` + ADR | PR #46의 회고 |
| 14 | **보류한 지적을 적을 자리가 없다.** PR 템플릿은 "보류한 지적은 **별도 티켓으로** 뺐다"를 요구하고 이 파일은 승인분만 담아, 회고 후보 단계의 것이 둘 다 아니다. PR #46에서 체크박스를 문자 그대로 만족하지 못해 본문에 단서를 달았고 claude-review가 독립적으로 같은 틈을 짚었다. 문언을 "티켓 또는 회고 후보로" 넓히거나 이 파일에 미승인 칸을 두거나. 5·10과 같은 파일이라 함께 간다 | `.github/PULL_REQUEST_TEMPLATE.md` | PR #46의 회고 + PR #46 claude-review |
| 15 | **세션 둘이 같은 체크아웃을 쓰는 것을 아무도 안 본다.** `operations.md`의 "티켓마다 워크트리 하나"에 판정자가 없다. PR #46에서 앞 세션의 커밋이 내 브랜치에 얹혔고 reflog를 읽고서야 알았다. **훅이 다른 에이전트 세션의 존재를 알 수 있는지 확인이 먼저다** — 불가능하면 함정 한 줄로 끝낸다. 지침 층은 PR #50이 `open-session` 6단계에 더했다("열고 나서 더 일하면 워크트리에서"). 판정자는 아직 없다 | 확인 뒤 `.claude/` 훅 또는 `CLAUDE.md` 환경 함정 | PR #46의 회고 |
| 16 | **`(traces, plugins_root)` Data Clump.** dataclass 셋(`RunArgs`·`ResumeArgs`·`ServeArgs`)과 함수 다섯 단계를 나란히 다닌다. 묶으면 `run`·`resume` 배관까지 번져 티켓 03 범위를 넘어 보류했다. 다음에 디렉터리 옵션이 하나 더 늘 때(`--config` 등) 같은 자리를 또 넓히지 않게 | `src/agent_os/channel/cli/main.py` | PR #46 리뷰 둘(claude-review, 셀프 리뷰 표준 축) |
| 17 | **`tools/open_session.ps1`에 회귀 시험이 없다.** PR #50이 `Normalize-Slash`·`Test-Needle`을 AST에서 떼어 열세 개로 시험하고 변이로 빨강을 봤지만 그 시험은 스크래치패드에 있었고 저장소에 없다. 다음에 같은 로직이 깨지면 다시 만들어야 한다. Pester는 새 의존성이라 `tech.md`가 ADR을 요구한다. 다른 길은 pytest가 `pwsh`로 AST 추출 시험을 부르는 것인데, `pwsh`가 없는 러너에서 skip하면 원칙 II의 "skip은 초록이 아니다"에 걸린다. **근거 추가**: PR #60에서 스킬이 넘긴 `focus -TimeoutSec 90`을 `Find-RowByTitle`의 10초 하드코딩이 조용히 무시하고 있었다(스크립트를 읽다 발견). 인자가 실제로 먹는지도 시험 대상이다. **3회차**: `chore/start-prompt-reads-skill-file`이 더한 `Convert-StartLine`(첫 줄의 스킬 명령을 스킬 파일을 읽으라는 한 줄로 옮긴다)도 AST로 떼어 경우 열하나와 변이 둘로 쟀는데 그 시험도 스크래치패드에 있다. 문자열을 받아 스킬 파일이 있는지만 보는 작은 함수라 첫 시험 대상으로 알맞다 | `tests/tools/` + (Pester면) `tech.md`·ADR | PR #50 claude-review Nit, 사용자 승인 2026-09-23, 일지 2026-09-23-06, 2026-09-24-02 |
| 18 | **`open-session`의 `stale=` 경로가 한 번도 실행되지 않았다.** PR #49가 넣은 스냅숏(쏘기 전에 이미 있던 매칭을 기억해 새로 생긴 것만 화면으로 친다)이 들어간 뒤의 실행 세 번(PR #49 병합 뒤 한 번, PR #50 셀프 테스트 두 번)에서 `stale=` 줄이 한 번도 찍히지 않았다. 틀려도 드러날 기회가 없었다. 확인하는 법: 앞 시도가 실패해 needle이 대화에 인쇄된 뒤 재시도해 `stale=N`이 먼저 찍히고 `page=`가 새 화면만 잡는지 본다. 17과 같은 파일이라 회귀 시험을 만들 때 함께 간다 | `tools/open_session.ps1` 실측 | PR #50의 "남긴 위험", 사용자 승인 2026-09-23 |
| 19 | **파이프 뒤 `$?` 경고 훅.** 게이트 명령(pytest·pyright·ruff·lint-imports·`tools/check_*`)이 파이프나 `&&` 체인에 묻힌 채 `$?`를 읽으면 PreToolUse에서 경고한다. 선례는 `tools/hook_bash_heredoc.py`·`hook_bash_python_stub.py`. PR #56 세션에서 `\| tail -3; echo $?`로 tail의 종료 코드를 읽었다 — `CLAUDE.md` 환경 함정에 적힌 것을 어긴 3회째라 지침이 아니라 훅이다. 11을 흡수한다. 거짓 양성이 많으면 막지 말고 경고만. **4회차**: PR #58 세션이 지침 검사를 `tail`에 넘긴 뒤 `$?`를 읽었다가 파이프 없이 다시 돌렸다 | `tools/` + `.claude/settings.json` | 일지 2026-09-23-04, 05 |
| 20 | **닫힌 수 주장을 리뷰가 민다.** `code-review` 명세 축 브리프에 한 줄: 문서·주석·ADR이 수를 세어 닫는 주장("둘뿐", "유일하다", "전부")은 반례를 찾아 민다. "길은 둘"(PR #56), "관대함은 하나"(PR #51), 면제 조항의 폭(그 앞)이 같은 가족 3회차이고 세 번 다 명세 축이 운으로 잡았다. **4회차**: PR #58의 ADR 초안 "끝이 잘린 파일은 단건에서도 `PluginError`가 아니다"가 빈 파일과 헤더 조각을 덮지 못했고 이번에도 명세 축이 잡았다. 수를 세는 주장만이 아니라 보편 주장("~은 ~가 아니다")도 같은 가족이다. **효과의 근거**: PR #63 세션이 표준 축 브리프에 "주석·독스트링의 사실 주장이 코드와 어긋나는지 본다"를 손으로 넣었더니 리뷰어가 주장 넷(import 순간의 `TypeError`, 평평한 `oneOf`, required, 한 줄 독스트링)을 프로브로 쟀다. 운이 아니라 브리프가 시킨 검증이었다. 이번에는 표준 축에 넣었으므로, 반영할 때 한 줄을 어느 축(또는 둘 다)에 둘지 정한다. **범위를 넓혔다 — 따옴표 인용도 같은 가족이다.** 설계 PR의 ADR이 인용 셋을 원문과 다르게 적었다(가운데 문장을 생략 표시 없이 건너뜀, 어순을 바꿔 따옴표 안에, 어디에도 없는 문장). 표준 축 브리프에 "인용한 문장이 그 문서에 실재하는지"를 손으로 넣어서 잡혔다. admin-api 설계(일지 2026-09-22-07)에서는 어미 하나가 달라 명세 축이 실재하는 스토리를 지어낸 것으로 오판했다. 한 줄의 문구: "따옴표 인용은 원문과 글자 그대로인지 grep으로 본다". **5회차(명세 세션)**: http-channel 명세에서 인용 둘(ADR 0011 이력 제목의 앞 절만, ADR 0010 이력 문장을 바꿔 따옴표에), 닫힌 수 하나(서두 "셋뿐"인데 본문 표시 다섯), ADR 문장의 범위를 넓혀 귀속한 것 하나. 표준 축 브리프에 인용을 **목록으로** 줬더니 목록의 여덟은 전부 검증되고 목록 밖의 둘은 명세 축이 운으로 잡았다 — 문구는 "**모든** 따옴표 인용"이어야 한다. `ADR NNNN의 날짜 "제목"` 꼴은 그 ADR의 `###` 제목과 기계로 대조할 수 있어 검사 후보다. **6회차(티켓 세션)**: 내 초안에서 인용 둘(ADR 0016 "…않고"를 "…않는다"로, 명세 "…재고,"를 "…잰다"로 끝맺어 따옴표 안에)과 닫힌 수 하나(명세 주석 "다섯 항목"). 표준 축(sonnet) 브리프에 "모든 따옴표 인용"을 넣었는데도 리뷰어는 16건을 대조해 "0건"이라 했고, ADR 이력 **본문 안의** 명세 인용은 그 16건에 없어 명세 축이 잡았다. 지침은 넣어도 운이다. **처방 후보를 넓힌다 — 기계 검사**: 바뀐 문서에서 긴 따옴표(예: 20자 이상, 짧은 것은 용어라 뺀다)를 뽑아 저장소에서 글자 그대로 찾고 없으면 경고한다. 막지 않고 경고만(거짓 양성: 번역·요약 인용). **7회차(버전 드리프트 세션)**: 일지 2026-09-24-03이 나열한 PR 번호 여섯과 http-channel 명세의 "PR 여섯"이 설계 PR과 명세 PR의 리뷰를 모두 지났는데 실제는 열하나였다. 헌법 README의 Last Amended 날짜(2026-09-21)를 기점으로 잡아 같은 날 병합된 #19~#30을 놓쳤다. 한 구절: "변경을 세는 기점은 날짜가 아니라 커밋이다(`git log <커밋>.. -- <경로>`)". **효과의 근거 하나 더**: 이번 셀프 리뷰는 두 축 브리프에 밀 주장을 하나하나 나열했고 두 축 모두 목록을 전부 쟀다. 명세 축은 대기열 31 초안이 `operations.md`를 근거로 `ci.yml`을 건드리면 Claude Code Review가 건너뛴다고 단정한 것이 같은 파일 바로 아래의 반례(PR #43)와 어긋나는 것을 잡았다. **8회차(http-channel 티켓 01)**: 두 축 브리프에 밀 주장을 나열했더니 표준 축이 같은 diff 때문에 거짓이 된 보편 주장 둘을 잡았다. 테스트 독스트링의 "run() 과 매니페스트를 읽는 자리가 같아서"(그 diff가 자리를 갈랐다)와, 검증하지 않는 "같은 진단과 종료 코드"다. 한 줄을 표준 축에도 두어야 한다는 근거다. **9회차(http-channel 티켓 02)**: 내 테스트 주석이 "`-` 는 실행 식별자 패턴이 받지 않아 `{run_id}` 를 받는 라우트가 생겨도 여기에 닿지 않는다"고 보편 주장을 했는데, `verbatim` 변환기가 슬래시까지 잡고 패턴 검증은 라우트가 맞은 뒤라 거짓이었다. 명세 축 브리프에 "03·04가 라우트를 붙이면 깨질 테스트"를 밀 주장으로 적었더니 명세 축이 변환기 정규식을 읽고 잡았다. 브리프 나열의 효과 근거가 하나 더다. **10회차(plugin-toggle 설계)**: ADR 0017이 ADR 0003의 "화면이 없어도 시스템이 돌고"를 "…돈다"로 끝맺어 따옴표 안에 넣었다. 표준 축(sonnet) 브리프에 "모든 따옴표 인용을 원문과 글자 단위로 대조"와 대조할 인용 넷을 적었더니 넷을 모두 쟀고 그중 이것을 잡았다. 같은 셀프 리뷰의 명세 축은 닫힌 수 하나("409는 두 경우", 실제는 형식 1까지 셋)를 잡았다 | `.claude/skills/code-review/` + (검사면) `tools/` | 일지 2026-09-23-04, 05, 2026-09-24-01, 03, 04, 05, 2026-09-25-01, 2026-09-26-01, 02, 06 |
| 21 | **칩은 병합된 것만 가리킨다.** 칩 워크트리는 스폰 시점의 main에서 뜬다. 피처 브랜치 세션이 그 브랜치에만 있는 파일을 가리키는 칩을 띄우면 그 세션은 없는 파일을 찾는다(PR #56, 1회차). 후보: 칩 프롬프트 첫 줄에 "<PR> 병합 뒤 main을 동기화하고 시작". **2회차**: PR #58 세션이 그 칩이었다. 워크트리가 05 없는 main에서 떠 지시문이 가리킨 ADR 이력과 테스트가 푸시 전 로컬 브랜치에만 있었고, 그 브랜치를 찾아 그 위에서 작업했다. 05가 병합된 뒤에는 사용자가 동기화를 짚었고 squash 전 원본을 빼는 `rebase --onto`가 필요했다 | 칩을 띄우는 자리의 지침 (`CLAUDE.md` 또는 `next-session`) | 일지 2026-09-23-04, 05 |
| 22 | **보이지 않는 줄 구분 문자 검사.** 편집 도구가 테스트 소스의 U+2028·U+2029 이스케이프를 날것 문자로 저장했다. pytest·ruff·pyright가 모두 통과했고, 표준 축 리뷰어는 문자가 보이지 않아 "U+2028 테스트가 없다"는 오탐을 냈다(PR #58, `grep`으로 찾았다). 고정된 문자 패턴이라 규칙이 아니라 검사다 — 추적 텍스트 파일에서 날것 U+2028·U+2029·U+0085를 잡는다. 선례는 `tools/check_type_escapes.py`. 1회차지만 기계적 위반이다 | `tools/` + `.pre-commit-config.yaml` | 일지 2026-09-23-05 |
| 23 | **티켓 밖 제품 코드 수정의 브랜치 접두사.** `operations.md` 브랜치 절은 `feature/<NN>-<slug>`(티켓), `chore/<slug>`(하네스·설정), `docs/<slug>`(문서)만 둔다. PR #58은 티켓이 아닌 제품 코드 수정이라 사용자 수준 `git-branch` 스킬의 `fix/`를 빌렸다. 한 줄: 티켓 밖 제품 코드 수정은 `fix/<slug>` | `docs/constitution/operations.md` | 일지 2026-09-23-05 |
| 24 | **발동하지 말아야 할 실제 입력으로 돌린다.** 모든 입력에 도는 훅(UserPromptSubmit, 매처가 넓은 PreToolUse)의 "실제 실행으로 확인"에 발동해야 할 입력만이 아니라 발동하지 말아야 할 **실제** 입력을 넣는다 — 그 훅을 만든 세션의 트랜스크립트에서 사용자 프롬프트를 흘려 출력이 없음을 본다. PR #60의 오탐은 사람이 우연히 지시문을 붙여 넣어 드러났고, 손으로 지은 반례("형식을 인용한 문장")는 턴이라는 축을 못 봤다. 10과 같은 줄이라 함께 간다 | `.github/PULL_REQUEST_TEMPLATE.md` | 일지 2026-09-23-06 |
| 25 | **훅의 stdin 은 바이트로.** `tools/hook_*.py` 넷이 텍스트 stdin이고, 훅 환경은 `PYTHONUTF8`이 없어 cp949·`surrogateescape`다. ASCII만 매치해 지금은 안전하지만 한글을 매치하는 훅은 예외 없이 exit 0·출력 0바이트가 되어 "발동 조건 아님"과 구별되지 않는다(PR #60에서 변이로 실측). 넷을 `json.load(sys.stdin.buffer)`로 바꾸고 `tools/check_instructions.py`에 "훅이 텍스트 stdin을 읽지 않는다" 한 항목. 1회차지만 실패가 조용하고 패턴이 고정돼 검사다 | `tools/hook_*.py` + `tools/check_instructions.py` | 일지 2026-09-23-06 |
| 26 | **명세 축에 넘기는 명세에서 요구와 참고를 가른다.** 티켓 없이 세션 요청을 명세로 쓸 때 붙인 "이 세션이 한 것" 목록을 명세 축이 요구로 읽어, 표준 축이 지우라고 한 문장을 "빠졌다"고 했다(PR #60). `code-review` 2단계에 "사용자 요청을 명세로 쓸 때 요구와 리뷰어 참고를 다른 절로" 한 줄. 1회차 | `.claude/skills/code-review/` | 일지 2026-09-23-06 |
| 27 | **설계 인터뷰의 라운드는 `AskUserQuestion`으로.** 사용자가 admin-api 설계에서 "UI로 선택할 수 있게 추천도 포함해서"라고 했는데(일지 2026-09-22-07) 그 선호가 일지 산문에만 있다. http-channel 설계 세션은 그 일지를 우연히 읽어 따랐고, `grilling`의 텍스트 형식과 UI를 둘 다 써 라운드가 두 벌이 됐다. 한 줄: "라운드는 `AskUserQuestion`으로 올린다. 추천을 첫 선택지로, 한 호출에 넷까지. 본문은 사실과 추천 근거만" **3회차(plugin-toggle 설계)**: `grill-with-docs`가 부른 `grilling`의 텍스트 형식을 그대로 따라 1라운드 아홉 문항을 본문으로 올렸다. 사용자가 "ui로 선택할 수 있게 추천과 함께"라고 다시 말해 같은 라운드를 UI로 한 번 더 올렸다. 이 세션은 앞의 두 일지를 읽지 않았다. 선호가 스킬에 없으면 세션마다 한 번씩 왕복이 든다. **우선순위**: 다음 `grilling` 세션(web-admin 설계) 전에 `chore/` 브랜치로 반영한다(사용자 승인 2026-09-26) | `.claude/skills/grilling/SKILL.md`(프로젝트 사본) | 일지 2026-09-24-03, 2026-09-26-06 |
| 28 | **retro 훅은 단계를 닫는 쓰기에만 발동한다.** `tools/hook_journal_retro.py`가 일지를 고치는 모든 Write·Edit에 "retro를 지금 돌린다"를 넣는다. 2회차다(일지 2026-09-22-07이 1회차로 적었다). 이번에는 retro를 시작한 직후 "검사" 절 한 줄을 고친 Edit가 다시 넣었다. 조건: Write이거나, Edit의 `new_string`이 `## 다음`을 담을 때. 훅을 만든 계기(2026-09-21, 일지를 쓰고도 돌리지 않음)는 Write가 덮는다. 기계적 조건이라 지침이 아니라 훅의 판정을 고친다. **3회차**: http-channel 명세 세션이 회고 결과를 일지에 덧붙인 Edit(`## 다음` 없음)에 다시 걸었다. **4회차**: 티켓 세션이 회고를 먼저 돌리고 일지를 Write한 뒤(여기는 조건대로 발동), 인용 다섯을 원문으로 고친 Edit(`## 다음` 없음) 다섯에 한 번씩 걸었다. **5회차**: 버전 드리프트 세션도 같은 순서였고, 인용 셋을 고친 Edit 셋에 걸었다 | `tools/hook_journal_retro.py` + 테스트 | 일지 2026-09-22-07, 2026-09-24-03, 04, 05, 2026-09-25-01 |
| 29 | **명세·ADR이 근거로 드는 프로브를 저장소에 남긴다.** 설계(03)와 명세(04)가 스크래치 프로브를 근거로 들었는데 스크립트는 세션 스크래치패드에 있어 사라진다. http-channel 명세는 "2026-09-24 프로브"를 다섯 번 들고, 그중 미들웨어 프로브는 둘째 구동자 테스트의 출발점이 될 모양이었다. 한 줄: "명세·ADR이 프로브를 근거로 들면 그 프로브를 `.scratch/<slug>/probes/`에 커밋한다". `ruff check .`가 `.scratch/`도 보므로 통과해야 한다. 이번 명세의 프로브 셋(`probe_mw_sse.py`, `probe_openapi_sse.py`, `probe_named_event.py`)은 반영할 때 함께 옮긴다. 자리는 `%LOCALAPPDATA%\Temp\claude\C--project-agent\39159309-b7a5-493a-9f48-af8dc203650e\scratchpad\`이고 임시 디렉터리라 남아 있을 때만 가능하다. 2회차. **3회차(2026-09-26-03)** — ADR 0014의 2026-09-26 이력이 fastapi 이분 탐색을, `channel/http/runs.py` 독스트링이 "이벤트 사이의 await" 프로브를 근거로 든다. 스크립트(`bisect_fastapi.py`, `probe_scope.py`, `probe_scope_cancel.py`, 변이 `mutate*.py`)는 `%LOCALAPPDATA%\Temp\claude\C--project-agent\67e6158e-290d-46fe-bab2-fa1688ab4776\scratchpad\`에 있다. **4회차(2026-09-26-04)** — 변이 스크립트를 또 스크래치에서 새로 지었고, 되돌리기를 `Path.write_text`로 해 Windows에서 줄 끝이 CRLF가 되어 손대지 않은 `core/run.py`가 수정된 것으로 보였다(바이트 단위 `read_bytes`/`write_bytes`로 고침). 반영할 때 **바이트 그대로 되돌리는 변이 도구**(파일·원문·치환·대상 테스트·반복 횟수를 받아 `finally`에서 복원)를 `tools/`에 두는 안을 함께 본다. 이번 스크립트(`mutate.py`, CI 탐침용 `wsl_ci.sh`·`docker_ci.sh`)는 `%LOCALAPPDATA%\Temp\claude\C--project-agent\6a3bbfd6-14de-4c77-93e9-4d3d573e3c63\scratchpad\`에 있다. **5회차(2026-09-26-05)** — 변이 다섯을 담은 `mutate.py`를 또 새로 지었다. 바이트 복원은 처음부터 맞췄지만 **아무것도 재지 않은 변이 둘이 초록으로 지나갔다**(도구 서버가 뜨기 전에 끝난 읽기 상한 0.5초, 요청이 그 안에 끝난 전체 상한 3초). 도구는 변이마다 **기대 결과(빨강/초록)를 받아 어긋나면 알린다**. 스크립트는 `%LOCALAPPDATA%\Temp\claude\C--project-agent\0697618f-606f-4a23-b99f-d42c32d6a380\scratchpad\`에 있다 | `docs/agents/issue-tracker.md` + `tools/` | 일지 2026-09-24-03, 04, 2026-09-26-03, 04, 05 |
| 30 | **to-spec 사본의 "파일 경로를 적지 않는다"를 저장소 관행에 맞춘다.** 선례 명세 둘(admin-api, interrupts)과 http-channel 명세가 모두 규칙 문서·ADR·테스트 선례의 경로를 적고, 표준 축은 그것을 스킬 문언 위반으로 낸다(PR 셀프 리뷰, Minor). 매번 보류할 오탐이다. 한 줄: "막는 것은 구현 파일 경로다. 규칙 문서·ADR·테스트 선례의 경로는 적는다". 원본(mattpocock/skills)과 다른 점을 알리는 주석을 다른 사본처럼 붙인다 | `.claude/skills/to-spec/SKILL.md` | 일지 2026-09-24-04 |
| 31 | **헌법 파일이 바뀌면 헌법 버전도 바뀌었는지 기계가 본다.** 거버넌스에 semver 문장이 있는데도 PR 열하나(#19~#50)가 버전을 올리지 않았다. 지침을 어긴 것이 세 번을 넘었으므로 검사 차례다. 조건: `principles.md`·`tech.md`·`operations.md` 중 하나가 바뀌었는데 `docs/constitution/README.md`가 안 바뀌면 실패. 거버넌스상 오타 수정도 patch라 PR 단위로는 규칙상 거짓 양성이 없다. 자리 후보가 둘이다. (1) pre-commit 로컬 훅(`files: ^docs/constitution/`, 넘어온 파일 이름으로 판정). 판정 단위가 커밋이라 헌법을 고친 커밋과 버전을 올린 커밋을 나누면 PR 전체로는 맞아도 앞 커밋이 빨강이다. CI는 pre-commit이 아니라 도구를 직접 부르므로 CI에서는 돌지 않는다. (2) CI의 merge-base diff(PR 단위). `actions/checkout@v4`가 얕은 복제라 `fetch-depth`를 바꿔야 하고, `ci.yml`을 건드리면 Claude Code Review가 건너뛸 수 있다(`operations.md`, PR #3 실측, 반례 PR #43, 조건 미상). 게이트가 느는 것이라 `operations.md` 가드레일 문장과 ADR이 먼저다 | `tools/` + `.pre-commit-config.yaml`·`ci.yml` + `operations.md` + ADR | 사용자 승인 2026-09-25, 일지 2026-09-25-01 |
| 32 | **`@` 임포트된 파일의 상대 경로는 루트 기준으로 읽힌다.** `principles.md`는 `CLAUDE.md`가 임포트해 루트 맥락에서 읽히는데, 형제 파일을 가리키던 `README.md`가 루트 README로 읽혀 PR #43이 헌법 버전을 찾지 못했다(대기열 9. 2.4.0은 그 두 자리만 전체 경로로 고쳤다). 같은 파일의 서두 `tech.md`·`operations.md`와 원칙 IV의 `adapters.md`도 같은 꼴이고, 루트에 같은 이름이 없어 무사할 뿐이다. `tools/check_instructions.py`에 한 항목: `CLAUDE.md`가 임포트하는 파일 안의 백틱 경로는 저장소 루트 기준으로 존재해야 한다. 무엇을 경로로 볼지(확장자나 `/`)는 반영할 때 정한다 — `agent_os.core`나 `Any` 같은 백틱은 경로가 아니다. 1회차지만 패턴이 고정돼 있어 검사다(선례 22·25). 반영하면 남은 상대 경로를 고치는 것이 헌법 문구 수정이라 patch를 올린다 | `tools/check_instructions.py` + 테스트 + `principles.md` | 사용자 승인 2026-09-25, 일지 2026-09-25-01 |
| 33 | **`open-session`이 연 새 세션의 Remote Control을 확인한다.** 딥링크로 연 `feature/02-channel-token` 세션이 시작 직후 `remoteControlActive: false`였고, 사용자가 모바일에서 그 세션을 찾지 못했다. 앱 설정 `connect_new_sessions_to_remote_control`은 켜져 있었다. 딥링크 세션에 그 기본값이 걸리지 않는 것인지, 조회(시작 약 3초 뒤)가 연결보다 빨랐던 것인지는 재지 못했다. 4단계(새 세션 확인)에서 `get_session`으로 `remoteControlState`를 보고 꺼져 있으면 `set_remote_control`로 켠다. 반영 전에 다음 열기에서 한 번 재서 둘을 가른다 — 연결이 늦는 것이면 켜기가 아니라 기다림이다 | `.claude/skills/open-session/` | 사용자 승인 2026-09-26, PR #70 세션의 병합 뒤 대화 |
| 34 | **선언된 의존성 하한을 재는 게이트가 없다.** CI는 `uv sync --frozen`으로 잠긴 버전만 돌아 `pyproject.toml`의 하한이 거짓이어도 초록이다. 티켓 03이 요구해서 잰 lowest-direct에서 fastapi 하한이 거짓이었고(#76이 바로잡음), `langchain-core>=0.3`(0.3.36에서 core 테스트 36개 실패)과 `langchain-mcp-adapters>=0.1`(mcp 상한이 없어 mcp 2.2.0에서 import 실패)은 지금도 거짓이다. 기계가 판정할 수 있어 검사다 — CI에 런타임 의존성만 lowest-direct로 푼 잡 하나(개발 도구는 락). 두 하한을 먼저 올려야 초록이고, 워크플로 변경은 별도 PR로 먼저 병합한다(`operations.md` 리뷰 파이프라인) | `pyproject.toml`·`uv.lock` + `.github/workflows/ci.yml` + `tech.md` | 사용자 승인 2026-09-26, 일지 2026-09-26-03 |
| 35 | **형제 티켓의 몫을 먼저 쓰지 않는다(3회차).** 1회차 일지 2026-09-26-01, 2회차 02(`ServeArgs` 독스트링), 3회차 03(`channel.md`의 "재개 라우트도 같다"). 셋 다 명세 축 리뷰가 잡았지만 고치는 한 바퀴가 든다. 기계로 가를 수 없어 지침이고 로드 시점은 구현이다 — `implement` 스킬에 한 줄: "이 티켓의 체크박스가 요구하는 것만 쓰고, 다음 티켓에 걸릴 것은 그 티켓 파일의 메모로 넘긴다." 적은 뒤에도 어겨지면 훅 후보다 | `.claude/skills/implement/SKILL.md` | 사용자 승인 2026-09-26, 일지 2026-09-26-03 |
| 36 | **CI가 인터프리터 충돌에서 단서를 남기지 않는다.** PR #78의 첫 실패 로그는 "545 passed" 뒤 종료 코드 139뿐이었다. pytest의 faulthandler는 세션이 끝나며 꺼지고 코어도 남지 않아, 로컬 19/19 초록 앞에서 CI를 루프로 탐침 아홉 번을 돌렸다(약 1.5시간). 결정적 단서는 여덟째의 코어 덤프 C 스택이었다. 정보 접근이라 검사다 — `ci.yml`의 `env`에 `PYTHONFAULTHANDLER: "1"`(종료 중 충돌의 파이썬 쪽 스택), 그리고 코어 두 곳. `ulimit -c unlimited`는 셸마다라 **pytest를 돌리는 그 `run` 단계 안에서** 걸고(`kernel.core_pattern`은 시스템 전역이라 앞 단계여도 된다), `if: failure()` 스텝은 남은 코어를 gdb로 열어 `bt`와 `info sharedlibrary`만 찍는다(PR #79 CodeRabbit). 탐침 셋째에서 `PYTHONFAULTHANDLER`가 재현을 없애지 않는 것을 봤다. 워크플로 변경은 별도 PR로 먼저 병합한다(`operations.md` 리뷰 파이프라인) | `.github/workflows/ci.yml` | 사용자 승인 2026-09-26, 일지 2026-09-26-04 |
| 37 | **PR 감시는 CodeRabbit의 "지적 없음" 완료를 알리지 않는다.** 앱의 감시가 깨우는 것은 CI 실패·충돌·새 리뷰 코멘트이고, CodeRabbit이 지적 없이 요약 코멘트만 고치고 끝나면 이벤트가 없다. PR #78에서 수정 커밋의 리뷰가 04:55에 끝났는데 세션은 06:48 사용자의 "끝이야?"까지 기다렸다. 행동이 일어나는 자리에 한 줄 — `next-session` 1단계: "리뷰를 요청한 뒤 다른 이벤트를 받을 때마다 `gh pr checks`의 CodeRabbit 줄(`Review completed`)을 함께 본다. 끝났는데 코멘트가 없으면 요약 코멘트의 `Reviewing files … between` 범위가 현재 HEAD까지인지 본다" **근거(2026-09-26-05)** — PR #80에서 손으로 지켰다. 수정 커밋의 재요청이 `Review rate limited`로 `pass`였고 요약이 `up to 4d35a`에 머문 것을 같은 확인이 잡았다. 한 줄이 "지적 없음"과 "한도 초과" 둘을 덮는다 | `.claude/skills/next-session/SKILL.md` | 사용자 승인 2026-09-26, 일지 2026-09-26-04, 05 |
