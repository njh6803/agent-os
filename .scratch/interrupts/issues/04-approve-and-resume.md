# 04: 승인하고 재개한다

**What to build:** 멈춘 실행을 승인하면 끝까지 간다. `agent-os resume <run_id> --approve`를 치면 런타임이 승인을 기록하고 에이전트를 처음부터 다시 돌리되, 이미 한 모델 호출과 도구 호출은 실제로 부르지 않고 트레이스의 기록된 값으로 재생해 멈췄던 자리까지 되돌아간 뒤, 거기서부터 진짜로 이어 간다. 트레이스 한 파일에 실행의 전모가 남는다.

기능의 심장이고 가장 큰 티켓이다. 재생이 성립하는 이유는 ADR 0008이 시각과 실행 식별자를 컨텍스트로 밀어 넣어 `run()`을 재실행 가능하게 만들어 뒀기 때문이다.

**Blocked by:** 03

**Status:** done

- [x] `core`에 재개 진입점이 생긴다. 실행 식별자와 결정과 승인자를 받고, 에이전트 이름·요청·주체는 받지 않고 트레이스의 시작 이벤트에서 읽는다
- [x] 승인이 트레이스에 먼저 기록된 뒤 재생이 시작된다
- [x] 재생 구간의 모델 호출과 도구 호출이 실제 포트에 **도달하지 않는다**(가짜 포트의 호출 횟수로 확인한다)
- [x] N번째 호출에 N번째 기록이 대응된다
- [x] 쓰기 전에 대조한다. 모델 호출의 입력 메시지와 도구 호출의 이름·인자가 기록과 다르면 실행이 실패로 끝난다. 마스킹된 필드는 대조에서 뺀다
- [x] 대조 실패의 내용이 무엇이 어긋났는지 말한다. 그 실행은 재개 불가가 되고 사용자는 새로 실행하면 된다
- [x] 재생 구간이 끝나는 자리에 재개 이벤트가 남는다
- [x] 재생된 사실은 트레이스에 다시 쓰이지 않는다. 에이전트가 낸 것도 마찬가지다. 기준은 "재생 구간인가"가 아니라 "이미 트레이스에 있는가"다
- [x] 재생 구간에서 **처음 생긴** 사실은 쓰인다. 대조 불일치로 끝나는 실행은 재개 이벤트 없이도 실패가 트레이스에 남는다
- [x] 시작 이벤트는 재개 시 다시 나지 않는다. 실행 하나에 한 번이고 트레이스는 한 파일에 이어진다
- [x] `ctx.now()`가 재생 구간에서 실행의 시작 시각을, 재개 이벤트 뒤로는 실제 시각을 돌려준다
- [x] 그 규칙과 "비결정적인 값은 전부 컨텍스트를 지나야 한다"는 제약이 `sdk`의 컨텍스트 문서에 적힌다
- [x] 루프 상한이 재생된 턴을 포함해 센다. 멈추고-재개를 반복해 무한히 돌 수 없다
- [x] 한 턴에 승인 대상이 둘이었다면, 첫 것을 승인해 재개한 뒤 **둘째에서 다시 멈춘다.** 승인과 실행의 1:1 대응이 여기서 닫힌다(03이 연 것의 나머지 절반이다)
- [x] 요청한 주체와 승인자가 같아도 통과한다. 자기 승인 금지는 이번 범위가 아니다
- [x] 일시정지가 아닌 실행, 이미 끝난 실행, 이미 실패한 실행을 재개하려 하면 거부된다
- [x] 형식 버전이 1인 트레이스를 재개하려 하면 거부된다
- [x] CLI의 `resume --approve`가 동작한다
- [x] 검사 넷이 초록이다

## Comments

- 2026-09-21(티켓 02 PR 직전 CodeRabbit CLI, Minor): `TraceStore.read()`는 파일 내용을 그대로 신뢰해 헤더의 `run_id`와 이벤트의 `run_id`가 어긋나도(손상·수동 편집) 그대로 돌려준다. 정상 쓰기 경로에서는 어긋날 수 없다. 트레이스를 재개의 입력으로 신뢰하는 것이 이 티켓이므로, 재개 진입점이 버전 1과 비일시정지를 거부하는 같은 자리에서 `run_id` 불일치도 손상된 트레이스로 거부한다. 02는 읽기만 한다.
  - 2026-09-22: 했다. `_sound_events`가 헤더와 이벤트의 실행 식별자 불일치, 모르는 종류의 이벤트, 빈 트레이스를 같은 자리에서 거부한다.
- 2026-09-22(구현): 계약을 한 줄 넓혔다. `LlmCalled.prompt`가 는다. 티켓이 요구한 "모델 호출의 입력 메시지" 대조가 그것 없이는 성립하지 않는다 — `LlmCalled`에 모델이 낸 것만 있고 넣은 것이 없었다. 사용자 승인을 받았고 근거는 ADR 0009의 2026-09-22 이력.
- 2026-09-22(구현): 재생이 드러낸 자리 셋을 ADR 0009 이력에 적었다. 에이전트가 낸 이벤트도 재생 기록에서 소비하는 것(04가 함), 마스킹된 인자가 재생 경계를 넘어 실제 도구에 갈 수 있는 것(04가 막음), 재개가 두 번 이상이면 `ctx.now()` 규칙이 스스로를 깨뜨리는 것(열린 문제).
- 2026-09-22(구현): 거부(`--deny`)는 티켓 05다. `Decision`은 `Approve` 하나이고 05가 `| Deny`를 더한다. CLI 의 상호배타 그룹도 자리만 만들어 뒀다. `_decision()`이 `--approve`를 실제로 읽으므로, 05가 거기를 고치지 않고 `--deny`만 더하면 거부가 승인으로 돌지 않고 진단이 난다.
- 2026-09-22(셀프 리뷰에서 남긴 것): `tests/core/test_run.py`의 `_run(agent, ..., plugins=...)`은 `plugins`가 주어지면 첫 인자를 버린다. 기존 호출 32개가 그 모양이고 이 티켓이 더 늘렸다. 에이전트 인스턴스를 두 번 만들어 무엇이 도는지 읽히지 않는다. 고치려면 `_run`을 둘로 쪼개고 호출부 32개를 바꿔야 해서 이 티켓의 diff 를 흐린다. 표준 축 Minor 이고 후속 허용이라 남긴다.
- 2026-09-22(셀프 리뷰에서 남긴 것, Nit): `_reject_masked_args`가 `Exception`이라 에이전트가 삼킬 수 있다. `_Paused`와 달리 `BaseException`으로 올리지 않은 이유는, 조립부의 `except Exception`이 그것을 `run_failed`로 바꾸는 자리라 `BaseException`이면 CLI 까지 그대로 나가기 때문이다. 삼켜도 검사가 호출 **앞**에 있어 마스킹된 값은 나가지 않는다. `ToolError`를 삼키는 것과 같은 층위다.
