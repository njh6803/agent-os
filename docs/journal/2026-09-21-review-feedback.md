# 리뷰 반영 루프 일지 · 2026-09-21

다른 프로젝트의 `AGENTS.md`에서 쓰던 "커밋 전 코드 리뷰 / 리뷰 피드백 구현" 블록을 이 저장소에 옮긴 기록. 하네스 셋(스킬, 헌법, 런북)을 고치고, 세션 중 회고가 검사 하나와 그 테스트를 더했다. 기록 규칙은 [킥오프 일지](2026-09-19-kickoff.md) 머리말과 같다.

> 사용자: "이 내용을 우리 프로젝트에 맞게 적용해줘. (내가 실제 다른 프로젝트에서 도움이 된 부분은 3.지적이 불분명하거나 기술적으로 맞지 않아 보이면 그대로 구현하지 않는다. 코드와 Spec에서 근거를 확인한 뒤 수정 여부를 정한다. 이 부분이야)
> 내용을 검토해보고 판단해줘. 만약 적용한다면 런북에도 초기에 설정할 것으로 넣어도 되는지도 확인해줘."

붙여 준 원문은 다른 프로젝트 `AGENTS.md`의 두 절(적용 범위·비교 범위를 정하는 "커밋 전 코드 리뷰"와, 보고 뒤 무엇을 하는지 정하는 "리뷰 피드백 구현")이다.

## 결과

- 아홉 항목 중 **넷 채택, 둘 부분 채택, 셋 거부**. 고친 파일은 `.claude/skills/code-review/SKILL.md`, `docs/constitution/operations.md`, `KICKOFF.md`, `tools/check_instructions.py` 넷이고 새 파일은 이 일지와 `tests/tools/test_check_instructions.py`다.
- 사용자가 지목한 항목(불분명한 지적은 근거를 먼저 본다)은 **채택**. 우리 저장소에서는 그것이 취향이 아니라 비대칭을 메우는 자리였다. `operations.md`에 봇의 "초록 착시"를 경계하는 문단이 다섯 줄 있는데 그 반대편이 비어 있었고, PR 직전 축에는 트리아지(`coderabbit-review`의 유효·오탐·보류)가 이미 있는데 커밋 전 축에만 없었다.
- **그보다 급한 갭을 검토 중에 찾았다.** 스킬이 `git diff <fixed-point>...HEAD`만 보는데 `implement`는 리뷰한 뒤 커밋한다. 커밋 전 셀프 리뷰인데 정작 리뷰할 것이 범위 밖이었다.
- 검사 넷과 지침 검사 초록. 문서만 바꾸는 변경이라 판정자는 검사가 아니라 실측이었다.

## 항목별 판단

| 원문 항목 | 판단 | 근거 |
|---|---|---|
| 커밋 전 리뷰 트리거, 서브에이전트 승인 | 거부 | `CLAUDE.md` 7, `operations.md` 리뷰 파이프라인 1, `implement` 스킬에 이미 셋 다 있다 |
| 비교 범위에 미커밋·미추적 포함 | 채택 | 아래 실측 |
| 문서 전용 변경은 리뷰 면제 | 거부 | 반대 결정이 이미 있다. 스킬 4단계가 문서 변경도 리뷰하되 모델을 sonnet으로 내린다(일지 09-20 첫 회고). 최근 커밋 다섯 중 셋이 `docs:`·`chore:`이고, `CLAUDE.md` 규약 4가 "하네스는 다음 실행에 바로 영향을 주는 코드"라고 못박는다 |
| `scripts/check_docs.py`로 문서 검사 | 거부(대체) | 그 자리는 `tools/check_instructions.py`이고 이미 pre-commit과 CI가 돌린다 |
| 보고로 끝내지 않고 수정까지 | 채택 | 의무는 `CLAUDE.md` 7에 있었지만 **리뷰 직후 읽는 자리**에 없었다. 스킬 6단계로 옮겼다 |
| 설계 결정·연결 계약은 먼저 묻기 | 채택(번역) | "연결 계약"을 우리 계약(`sdk/`, `openapi.json`, 헌법)과 ADR 승인으로 바꿨다 |
| 판단 항목이 커밋을 막지 않음 | 부분 채택 | 심각도 넷으로 이미 표현된다. 없던 "남긴 이유 한 줄"만 더했다 |
| **불분명한 지적은 근거 확인 후 결정** | 채택 | 아래 |
| 수정 뒤 검증 재실행, 전체 리뷰는 안 돌림 | 채택 | 검증 명령은 있었으나 이 계기가 없었고, 리뷰 루프가 도는 것을 막는 상한도 없었다 |

## 범위 갭은 실측으로 확인했다

이 세션의 변경이 전부 미커밋 상태라 그 자체가 시험대였다. 같은 base에서 두 규칙을 돌렸다.

```
BASE=d20c9e7
git diff --stat "$BASE...HEAD"   → (빈 결과)
git diff --stat "$BASE"          → 4 files changed, 56 insertions(+), 10 deletions(-)
git status --porcelain | grep ^?? → 2개(이 일지, 새 테스트)
```

이 세션은 커밋이 0개라 옛 세 점 규칙에서는 **빈 결과**가 나온다. 리뷰가 시작조차 못 한다. 가설이 아니라 실측이다. 미추적 둘은 어떤 diff에도 안 잡혀서 `git status --porcelain`의 `??`를 따로 넘긴다.

1단계에 SHA 고정을 넣은 것은 새 기능이 아니라 범위 확장의 구현 수단이다. 이유는 셸 호환이 아니라 **기준점**이다. `git diff`는 ref도 SHA도 받고 PowerShell의 `$()`도 정상 동작한다(PR #27 리뷰 중 실측. 처음에 셸 호환이라고 적었다가 CodeRabbit의 지적으로 확인하고 고쳤다). 진짜 이유는 `git diff main`이 "지금의 main"과 비교라는 것이다. 세션 중 main이 전진하면 main의 변경이 역방향으로 섞이고, 서브에이전트 둘이 각자 `git merge-base`를 다시 돌리면 서로 다른 나무를 볼 수 있다.

## 근거 확인을 넣은 이유

서브에이전트 둘은 격리된 컨텍스트에서 diff와 표준 문서만 본다. ADR도 주변 코드도 모른다. 특히 smell baseline은 스킬이 스스로 judgement call이라고 말하는 휴리스틱이고, 우리 아키텍처가 정확히 그 휴리스틱에 걸리는 모양이다. 포트와 어댑터의 간접층은 Middle Man으로, 원칙 IV가 요구하는 경계는 Speculative Generality로 보인다. 오탐을 그대로 고치면 **수정이 원칙을 깬다.** 봇의 초록을 믿지 않는 것과 같은 이유로 빨강도 그대로 믿지 않는다.

## 런북에는 넣었다

기준은 "그 내용이 어딘가에 있나"가 아니라 "행동이 일어나는 자리에 있나"(런북 규칙 1)다. 두 자리에 넣었다.

- **7단계** 파일 목록. 이미 "런북 저장소에서 복사해 검증 명령만 바꾼다"로 일곱을 나열하는 자리다. 여덟째로 `code-review` 사본에 덧댈 둘(범위, 반영 절차)을 근거와 함께 넣었다. 첫날에 하는 것이 맞는 이유는 이것이 되돌리기 싼 변경이어서가 아니라, 9단계의 첫 리뷰보다 먼저 있어야 리듬의 일부가 되기 때문이다.
- **9단계 6번.** 기능마다 반복해서 읽히는 순서다. 범위와 근거 확인을 한 줄씩 더했다.

부록 A의 함정 표에는 넣지 않았다. 그 표는 환경·도구가 조용히 통과하는 것을 모은 자리이고, 이것은 함정이 아니라 절차다.

## 회고 (일지를 쓰며 훅이 걸려서)

> 사용자: "한 번에"

훅이 계기를 넣어 retro가 스스로 돌았다. 지난 세션에 이어 두 번째다. 가드레일 현황부터 봤고(훅 여섯 + commit-msg, 놀거나 깨진 검사 없음) 새 검사를 지어낼 자리가 아니었다. 후보 셋 중 둘을 사용자가 승인했다.

1. **덧댄 스킬 사본이 `npx skills update -p`에 덮어써지는 것을 검사로 잡는다.** 이번에 code-review의 덧댄 지점이 하나에서 셋으로 늘었는데, 되돌아가면 범위와 반영 절차가 조용히 사라지고 리뷰는 그 뒤로도 초록이다. 부록 A에 주의는 있었지만 사람이 기억해야 했다. **장치를 새로 만들지 않았다** — 사본 셋(`code-review`, `implement`, `retro`)에 이미 `<!-- 프로젝트 사본 ... -->` 주석이 있었고 고정 문자열이라 기계가 판정할 수 있었다. `tools/check_instructions.py`에 23줄.
2. **리뷰 보고에 본 범위 한 줄.** 범위가 어긋난 리뷰는 실패하지 않고 초록으로 끝난다. 부록 A의 "조용히 통과하는 것을 시끄럽게 만든다"를 리뷰 자신에게 적용한 것이다.

승인받지 못한 것: `CLAUDE.md` 7번에서 중복된 심각도 문구를 빼는 것. 기록만 한 것: `Path.write_text`가 Windows에서 `\n`을 `\r\n`으로 쓴다는 것(LF 파일에 쓰면 diff가 파일 전체가 된다. 이번엔 `KICKOFF.md`가 원래 CRLF라 왕복이 우연히 맞았다), `KICKOFF.md` 373줄에 단계 목차가 없다는 것. 둘 다 한 번의 사건이라 규칙으로 올리지 않았다.

## 셀프 리뷰가 자기 자신을 잡았다

새 6단계와 새 범위의 첫 시험대가 이 변경 자신이었다. 1단계가 커밋 0개·미커밋 4파일·미추적 1개를 잡았다. 두 축이 Major 둘, Minor 셋, Nit 둘을 냈고 **두 축이 독립적으로 같은 것 넷을 짚었다.**

고친 것 다섯.

1. **일지가 이 변경을 잘못 기록하고 있었다**(양쪽 Major). 일지를 쓴 시점의 파일 수와 실측 숫자가 그대로 남아 회고 반영분을 빠뜨렸다. 지금 이 절들이 그 수정이다.
2. **스킬 1단계가 자기모순이었다**(양쪽). 새 한국어 문장 바로 뒤에 원문 `A bad ref or empty diff should fail here`가 살아 있었다. 미추적만 있고 diff가 빈 상태 — 이 변경이 닫으려던 바로 그 갭 — 에서 리뷰가 중단됐을 것이다.
3. **런북이 세 자리에서 낡았다**(양쪽). 3단계의 "훅이 막는 것 셋" 나열, 부록 A의 "갱신 뒤 확인"(이제 검사가 한다), 7단계의 "둘을 덧댄다"(셋이다). 세 번째는 새 프로젝트가 런북을 따르면 5단계 범위 줄을 못 받는다는 뜻이었다.
4. **새 검사에 테스트가 없었다.** 변이로 빨강은 봤지만 일회성이라 회귀를 막지 못한다. `tests/tools/test_check_instructions.py` 넷을 먼저 빨갛게 쓰고 통과시켰다.
5. 독스트링이 "첫머리의 주석"이라 했지만 검사는 파일 전체 substring이다. 문구를 사실에 맞췄다.

**지적을 그대로 구현하지 않은 것 하나.** 표준 축이 "동료 도구 셋은 전부 테스트가 있는데 이 함수만 없다"고 했다. 근거를 보니 `tools/`의 다섯 중 셋만 테스트가 있고 `check_instructions.py`는 원래부터 없었다. 서브에이전트가 `tests/tools/`만 보고 `tools/`와 대조하지 않은 것이다. 전제는 틀렸지만 결론(테스트가 있어야 한다)은 원칙 II가 독립적으로 요구하므로 받아들였다. **6단계 2번이 처음 작동한 자리다.**

남긴 것 넷과 이유.

- `operations.md`가 범위와 면제 정책을 적은 것이 "가리키면서 요약했다"는 지적(Minor). 정책은 헌법 자리이고 절차만 스킬에 있다. 경계가 흐린 것은 맞지만 옮기면 정책이 스킬에 숨는다.
- 스킬 4단계의 한/영 혼합(Nit). 원본과의 diff 면적을 줄이려는 의도된 형태다. `implement` 사본도 같다.
- `patched_skills_without_sentinel()`이 "파일 없음"도 보고하는 것이 Mysterious Name이라는 지적(Nit). 파일 없음은 센티널 소실의 극단이라 같은 질문에 대한 답이다.
- SHA 고정이 명세에 없던 추가라는 지적(판단). 범위 확장의 구현 수단이다. 근거를 위 실측 절에 적었다.

다시 돌린 명령: `ruff check`·`ruff format --check`·`pyright`·`lint-imports`·`pytest`·`check_instructions.py`. 전부 초록(`99 passed`, `0 errors`, `Contracts: 3 kept`). 전체 리뷰는 다시 돌리지 않았다.

도중에 `pyright`가 `set`의 불변성을 짚어 테스트 헬퍼의 인자를 `Container[str]`로 바꿨다. 원칙 III대로 `cast` 없이 타입으로 풀었고, 오류 메시지가 해법까지 적어 줬다.

## PR 봇이 초록이면서 지적을 남겼다

검사 셋(`CodeRabbit`, `claude-review`, `verify`)이 모두 `pass`인데 코멘트에는 지적이 셋 있었다. 초록 착시의 반대 경우다 — `pass`가 "지적 없음"을 뜻하지 않는다. 병합 전에 코멘트를 본다는 규약이 여기서 값을 했다.

**고친 것 하나.** CodeRabbit이 위 실측 절의 설명을 짚었다. "`git diff`는 ref와 SHA를 모두 받고 PowerShell도 `$()`를 지원한다." 확인해 보니 둘 다 맞았다. PowerShell에서 `git diff --stat $(git merge-base main HEAD)`가 정상 동작했고 `git diff main`도 그대로 받는다. **내가 이유를 잘못 적었다.** 진짜 이유(기준점 고정)로 고쳤고 스킬 1단계의 "셸이 다시 확장하지 않고"도 같이 고쳤다.

**보류 둘과 이유.**

- **센티널을 frontmatter 뒤 첫 주석으로 검사하라**(CodeRabbit Minor). 전제는 맞다. 지금 검사는 파일 전체 substring이라 본문 아무 데나 있으면 통과한다. 그런데 위협은 "원본이 사본을 덮어쓴다"이고 **원본은 영문 스킬**이라 한국어 센티널이 본문에 남을 경로가 없다. 센티널은 지금 파일당 1회다. 반대로 frontmatter 파싱을 더하면 검사가 스킬 파일 형식에 결합되어, frontmatter 없는 사본을 덧대는 날 거짓 양성이 난다. 이건 pre-commit 하드 게이트이고 `CLAUDE.md`는 "거짓 양성이 많은 규칙을 하드 게이트로 만들지 않는다"고 한다. 셀프 리뷰도 같은 불일치를 짚었지만(Nit 6) 나는 독스트링을 구현에 맞추는 쪽으로 이미 해결했다.
- **`operations.md`와 스킬의 중복 서술**(Claude Code Review Minor). 한 번 보류했다가 다음 푸시에서 고쳤다. 아래.

## 보류했다가 고친 것

중복 서술을 처음엔 "정책은 헌법, 절차는 스킬"이라는 근거로 남겼다. 반영분을 푸시하자 Claude Code Review가 다시 돌며 같은 자리를 짚었고, **이번에는 논증이 달랐다.** 같은 파일 42행이 "같은 사실을 두 곳에 두지 않고, 지역 문서는 루트를 요약하지 않고 가리킨다"고 적고 있다는 것이다. 20행을 문장 단위로 갈라 보니 맞았다. 정책(범위·면제·심각도)과 절차 요약이 섞여 있었고, "`HEAD`까지만 보면 빠진다"는 스킬 1단계의 이유, "근거를 먼저 확인"은 6단계의 절차다. 내 근거는 정책 문장만 정당화하지 그 둘까지는 못 한다. 절차 문장을 빼고 "범위를 잡는 법과 반영 절차는 스킬 1·6단계"로 가리키게 고쳤다.

**메타 논증은 받지 않았다.** Claude는 "작성자가 세 번째면 고친다고 적었는데 이번이 세 번째"라고 했지만, 2·3회차는 같은 봇의 재실행이다. 같은 프롬프트에 같은 코드면 같은 결론이 나오고, 그것은 독립 관찰이 아니라 한 번의 관찰을 두 번 읽은 것이다. `CLAUDE.md` 교정 루프가 세는 것은 사건의 반복이지 관찰의 반복이 아니다. 내 기준으로는 아직 두 번이다. **고친 이유는 횟수가 아니라 새로 나온 근거(42행)다.** 봇이 내 기준을 인용할 때도 그 적용이 맞는지는 내가 본다.

## 다음

- 커밋과 PR. 브랜치는 `chore/review-feedback-loop`.
- 사본이 되돌아갔는지 확인하는 일은 이제 훅이 한다. 사람이 기억할 항목이 하나 줄었다.
- 이 변경은 티켓이 없는 하네스 개선이라 `.scratch/plan.md`의 프론티어 셋(http-channel, admin-api, interrupts)은 그대로다.
- 다음 리뷰에서 5단계 범위 한 줄이 실제로 보고에 나오는지 본다. 이번 보고에는 내가 손으로 적었다.
