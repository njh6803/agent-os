# 킥오프 일지 · 2026-09-19

런북 [KICKOFF.md](../../KICKOFF.md)를 따라 진행한 기록. 단계마다 한 일, 결과, 런북에서 벗어난 점을 적는다.

기록 규칙: 결정을 내리거나 방향을 바꾸거나 설계를 되물은 사용자 프롬프트는 해당 단계 아래에 `> 사용자:` 인용으로 원문 그대로 남긴다. 단순 조작 지시는 남기지 않는다. 에이전트의 말은 옮기지 않고, 추천과 결정이 갈린 곳과 에이전트가 번복하거나 고친 것만 세션 끝 절에 한 줄씩 남긴다. `/retro`의 입력이다.

## 배경

> 사용자: "제미나이가 설명해준건데 단계적으로 분석하고 검토해줘. 내가 만들 프로젝트의 이름 Agentic OS라고 해도 될까?"

- 제미나이 대화 PDF("개발자의 에이전트 빌더 활용도", 16쪽)를 분석했다. 방향은 공통 런타임 엔진 위에 에이전트 워크플로우와 MCP 스킬을 플러그인으로 갈아끼우는 구조.
- 검토에서 나온 설계 지침 세 가지:
  1. "7가지 실행 패턴"은 정의 형식(코드 / 선언적 그래프 / DSL), 실행 위치와 격리(in-process / 서브프로세스 / 컨테이너 / WASM / FaaS), 생성 시점(사전 정의 / 실행 시 생성) 세 축으로 나눠 조합한다. 로더를 7개 만들지 않는다.
  2. 에이전트 인터페이스는 단일 호출 `invoke(state) -> state`가 아니라 이벤트 스트림. 스트리밍, 승인 대기 인터럽트, 취소, 트레이싱이 여기서 나온다.
  3. 툴(모델이 호출하는 함수), 스킬(모델이 읽는 절차와 자료), MCP(외부 시스템 연결 프로토콜)는 세 층으로 분리한다.
- 이름: "Agentic OS"는 비전 문구로만 쓴다. 이미 같은 이름의 저장소와 조직이 GitHub에 여럿 있고, OS라는 말이 약속하는 범위가 MVP보다 크다. 저장소와 패키지 이름은 고유 코드네임으로, 헌법 인터뷰에서 결정한다.
- 미결: 옆 폴더 `agent-runtime-platform`, `ai-agent-platform`과의 관계(세 번째 시도인지, 통합인지). 헌법 인터뷰에서 결정한다.

## 0단계. 전역 준비

> 사용자: "KICKOFF.md 파일 읽고 단계별로 시작하자. 그리고 내가 진행하는 이 과정들을 기록하고 싶어."

확인만 했다. 이미 적용된 상태였다.

| 항목 | 상태 |
|---|---|
| superpowers 플러그인 | disabled (v6.3.0) |
| 전역 `~/.claude/CLAUDE.md` | 0바이트 |
| 전역 스킬 | grill-me, grilling 외에 git-*, jira-*, synced 존재 |
| `~/.claude/skills-backup/` | domain-modeling, grill-with-docs 이동 완료 |

벗어난 점: 전역에 git-*, jira-* 스킬이 남아 있다. 런북의 "grill-me, grilling만" 원칙은 mattpocock 스킬과의 이름 충돌을 막기 위한 것이고, git/jira 스킬은 이름이 겹치지 않으며 4단계 이슈 트래커 흐름에서 쓰이므로 그대로 둔다.

## 1단계. 저장소

- `git init -b main`. 브랜치 이름을 main으로 지정한 이유: 런북 9단계가 `/code-review main`을 쓴다.
- `docs/`, `docs/adr/`, `docs/agents/`, `docs/journal/` 생성.

## 2단계. 스킬 설치

- `npx skills@latest add mattpocock/skills -a claude-code --copy -y -s ...` 12개 한 번에 성공. `.claude/skills/`에 복사됨.

## 3단계. CLAUDE.md

- 런북 템플릿을 그대로 썼다. `/init`은 건너뛰었다. 빈 저장소라 `/init`이 만들 내용이 없고 목표 모양이 런북에 있기 때문.
- 프로젝트명은 `agent (가칭)`. 헌법 인터뷰에서 확정하면 바꾼다.
- 지도에 `진행 일지: docs/journal/` 한 줄을 추가했다.

## 5단계(준비). 헌법 템플릿

- Spec Kit constitution 템플릿을 `docs/constitution.md`로 내려받았다. 50줄. 채우는 인터뷰는 아직.

## 6단계. CODING_STANDARDS.md

- 런북 뼈대 그대로. 판단 기준 비어 있음.

## 4단계(준비). setup-matt-pocock-skills 탐색

- 스킬은 `disable-model-invocation: true`. 절차는 탐색, 질문, 초안 확인, 쓰기 순.
- 탐색 결과: git remote 없음. `CLAUDE.md` 있음(여기에 `## Agent skills` 블록 추가). triage 미설치(라벨 절 생략). 모노레포 신호 없음(단일 컨텍스트).
- Jira 접근 확인: `njh4803.atlassian.net`. 프로젝트 두 개. AAPP(AI Agent Platform Project), ARP(Agent Runtime Platform). 옆 폴더 두 프로젝트와 1:1 대응.
- grill-me는 grilling을 호출하는 한 줄짜리 래퍼. 5단계 인터뷰는 grilling으로 진행.
- 결정 대기: 이슈 트래커. 기존 Jira 프로젝트 재사용인지, 새 프로젝트인지가 옆 폴더와의 관계 결정과 묶여 있다.

## 4단계. setup-matt-pocock-skills 실행

> 사용자(선택지 답): "로컬 마크다운"

- 결정: 이슈 트래커는 **로컬 마크다운**(`.scratch/<feature-slug>/`). Jira의 ARP/AAPP를 재사용하지 않았다. 이 저장소는 옆 프로젝트와 별개로 출발한다는 신호.
- 스킬은 사용자 호출 전용(`disable-model-invocation`)이라 SKILL.md의 절차를 에이전트가 수동으로 따랐다. 탐색, 결정 질문, 쓰기.
- 산출물: `docs/agents/issue-tracker.md`, `docs/agents/domain.md`, `CLAUDE.md`의 `## Agent skills` 블록. 모두 한국어.
- 벗어난 점 1: 템플릿의 `/wayfinder` 절은 스킬이 설치되지 않아 뺐다. 설치하면 템플릿에서 다시 가져온다.
- 벗어난 점 2: `Status:` 값은 triage 스킬이 없어 이 저장소에서 직접 정했다. to-tickets가 `ready-for-agent`를 쓰므로 그 어휘를 유지하고 `in-progress`, `done`, `wontfix`를 더했다.
- 결정: `.scratch/`는 커밋한다. 유일한 작업 기록이기 때문.

## 5단계(사실 조사). 옆 프로젝트 조사 결과

서브에이전트가 두 저장소를 읽고 보고한 것 중 결정에 영향을 주는 사실만 남긴다.

- `ai-agent-platform`: 2026-08-30~09-10, 11일, 113커밋, 41,100줄. 그래프 빌더 전제(노드 12종, CEL, 캔버스). LLM 호출 코드 0줄. 중단.
- `agent-runtime-platform`: 2026-09-17~18, 이틀, 15커밋. ADR 0001로 위 저장소를 이어받지 않고 재작성. ADR 0002로 "에이전트는 그래프가 아니라 코드 플러그인". Python 3.12 고정, uv, ruff(DTZ·TD003을 헌장 판정자로), mypy strict, import-linter, pytest. FastAPI+SQLAlchemy+Alembic walking skeleton, 매니페스트 SDK(name / version / entrypoint `모듈:속성`). 로더·실행 루프·LLM 호출 코드 0줄. CHARTER 미정 24항, 게이트 24행, 가드 훅 넷. 철수 조건: 연속 10일 커밋 0.
- 패턴: 세 저장소가 3주 안에 시작됐고, 앞의 둘은 LLM 호출 0줄에서 멈췄다. 이 저장소의 가장 큰 위험은 설계가 아니라 같은 패턴의 반복이다.
- 상속 후보(코드가 아니라 결정): 코드 플러그인, 매니페스트 entrypoint 형식, 시크릿 기록 금지(D4), 낙관적 잠금.
- 상속할 환경 함정: `PYTHONUTF8=1` 필수. PowerShell here-string `@'...'@`이 `@`를 커밋 메시지에 흘림(지침으로 안 돼서 훅으로 차단). 파이프 뒤 `&&`는 파이프 마지막 명령의 종료 코드만 봄. ini 계열 파일은 ASCII만. ruff E501은 표시 폭 기준이라 한글이 2로 세어짐. Docker가 없으면 통합 테스트가 skip으로 초록.

## 5단계. 헌법 인터뷰 1라운드 답

> 사용자: "1. A / 2. 그냥 agent os? agentic os? 라 하자 / 3. A / 4. A / 5. A"

- Q1 옆 프로젝트와의 관계: **A. 새로 시작.** 코드는 가져오지 않고 결정과 교훈만.
- Q2 이름: 코드네임(maru) 대신 **"Agent OS" 또는 "Agentic OS"** 중 하나로. 확정은 2라운드.
- Q3 첫 슬라이스: **A. 최소.** in-process 로더, 이벤트 스트림 인터페이스, LLM 프로바이더 1개(Anthropic), MCP stdio 서버 1개, JSONL 트레이스. 그래프·원격·메모리·UI·스케줄러 제외.
- Q4 형태: **A.** 순수 라이브러리 코어 + CLI. HTTP는 두 번째 슬라이스의 어댑터.
- Q5 스택: **A.** Python 3.12, uv, ruff, pyright strict, pytest. 선행 두 저장소는 mypy strict였음. 2라운드에서 재확인.

## 5단계. 헌법 인터뷰 2라운드 답

> 사용자: "1. A / 2. A / 3. mcp도 플러그인 형태로, 스킬도 플러그인형태로 갈거고, src/agent_os/ channel/cli 는 어때? /plugins/agents 는 어때? 저장소 구조는 나중에 바꾸기 어렵나 / 4. 그대로 / 5. 그대로"

- Q1 이름: **A. Agent OS.** 슬러그 `agent-os`, 패키지 `agent_os`. CLAUDE.md 제목 반영.
- Q2 타입체커: **A. pyright strict.**
- Q3 원칙 다섯: 답 없음(번호가 밀린 것으로 보임). 3라운드에서 재확인.
- Q4 구조: 사용자 제안 셋. (1) MCP와 스킬도 에이전트처럼 플러그인. (2) CLI를 `src/agent_os/channel/cli`로, "channel"이 호출면의 이름. (3) 루트 `agents/` 대신 `plugins/agents/`. 질문: 구조는 나중에 바꾸기 어려운가.
- Q5 검증·가드레일·브랜치: 그대로.
- Q6 거버넌스·철수 조건: 그대로.

## 5단계. 헌법 인터뷰 3라운드 답과 헌법 작성

> 사용자: "1. 그래로 / 2. 그대로 / 3. A"
> 사용자(헌법 초안에): "승인"

- Q1 원칙 다섯: 그대로. 4번에 "플러그인은 sdk만 import" 추가.
- Q2 구조: 그대로. `sdk ← core ← channel`, `plugins → sdk`. `plugins/{agents,mcp,skills}`, `plugin.toml` 하나에 `kind`.
- Q3 스킬: **A.** 스킬은 절차 지식(SKILL.md 폴더). 툴 경로는 MCP 하나.
- 프론티어가 비어 `docs/constitution.md` v1.0.0 작성. 템플릿 자리표시자 0개.
- 에이전트가 정한 소소한 것 셋(사용자 검토 대상): (1) LLM 테스트는 `llm` 마커, 기본 실행에서 제외, 키 없으면 skip 아닌 실패. (2) Python은 `==3.12.*` 고정. (3) `CLAUDE.md`가 `@docs/constitution.md`로 임포트해 매 세션 원칙이 읽히게 함. 런북이 허용한 선택지.

## 7단계. 스캐폴딩과 검증 명령

- 생성: `pyproject.toml`(uv, hatchling, `==3.12.*`, anthropic·mcp·pydantic, dev는 ruff·pyright·pytest·import-linter·pre-commit), `src/agent_os/{sdk,core,channel/cli}`, `plugins/{agents,mcp,skills}`, `tests/test_manifest.py` 샘플 3개, `.pre-commit-config.yaml`, `.gitignore`, `.env.example`, `README.md`.
- sdk: `manifest.py`에 PluginManifest(kind, name, version, entrypoint는 agent만). `events.py`와 `agent.py`는 BaseEvent와 빈 BaseAgent 프로토콜만 두고 나머지는 사용자 몫으로 TODO를 남김.
- 설치된 버전: anthropic 1.7.0, mcp 2.2.0, pydantic 2.13.5, ruff 0.16.8, pyright 1.1.414, pytest 9.1.1, import-linter 2.15.
- 검증: pytest 3 passed. ruff check 통과. ruff format은 테스트 파일 1개 따옴표 수정 후 통과. pyright 0 errors. lint-imports 계약 1 kept. `pytest -m llm`은 3 deselected, exit 5로 마커 오버라이드 확인. CLI `--version` 0.1.0.
- 함정(겪은 것, 2026-09-19): `pre-commit run --all-files`가 추적 파일 0개 상태에서 훅 5개를 전부 "no files to check / Skipped"로 넘기고 exit 0. 거짓 초록. `git add` 뒤 다시 돌려 5개 전부 실제 통과 확인. 대책: pyright·import-linter·pytest 훅에 `always_run: true`. 파이썬 파일이 안 바뀐 커밋(pyproject만 바뀐 커밋)에서도 돌게 됨.
- `.gitattributes`로 줄끝을 LF로 고정. autocrlf 경고 22건 제거.
- `CLAUDE.md` 검증 명령 칸 채움. 28줄.

## 8단계. 첫 커밋

- `7aca1dc` chore: 하네스 킥오프. 커밋 시 pre-commit 훅 5개 통과. 원칙 I의 3일 기한 시작.

## sdk 계약 작성 (8단계 뒤, 사용자 위임)

> 사용자: "sdk 네가 써"

- 사용자가 sdk 작성을 에이전트에게 위임. TDD로 진행: `tests/test_events.py`, `tests/test_agent.py`를 먼저 써서 ImportError로 빨강 확인, 구현 후 7 passed.
- 결정 셋(`events.py`, `agent.py` 머리말에 날짜와 함께 기록):
  1. 이벤트는 종류별 클래스 + `Literal` type 필드의 판별 유니온. 종류 다섯: run_started, llm_called, tool_called, run_finished, run_failed. `ts`는 `AwareDatetime`으로 naive 거부.
  2. `BaseAgent.run(request: str, ctx: AgentContext) -> AsyncIterator[Event]`. 입력은 문자열 하나.
  3. LLM과 도구는 `AgentContext`(llm, tool 메서드 둘)로 run()에 주입. 플러그인은 sdk만 본다.
- 테스트의 `# noqa: DTZ001` 한 곳은 naive datetime이 거부되는지 확인하려고 일부러 만든 것. 원칙 III(타입 우회 금지)의 대상이 아니다.
- `e4ed842` feat: sdk 이벤트 타입과 BaseAgent 프로토콜 정의. 훅 5개 통과.

## API 키 경로 결정

> 사용자: "커밋해줘. ANTHROPIC_API_KEY는 어디서 발급 받아?"

- 이 머신에 `ant` CLI 없음, 환경변수 `ANTHROPIC_API_KEY` 미설정. 키는 Anthropic Console에서 발급해 `.env`에 넣는다(사용자가 직접). uv는 `.env`를 자동으로 읽지 않으므로 LLM 테스트 명령은 `uv run --env-file .env pytest -m llm`. `.env.example`로 `--env-file` 동작 확인.

## 9단계. 첫 슬라이스 설계 인터뷰 (grill-with-docs)

> 사용자(1라운드 도중): "근데 나는 LangGraph 쓰고 싶은데"
> 사용자(세 가지 뜻 중): "가"

- 사실 조사: `CONTEXT.md`, ADR 없음. `@modelcontextprotocol/server-everything` npm에 존재(2026.8.31). 설치된 anthropic 1.7.0에 `beta.messages.tool_runner`(async)와 MCP 브리지 `async_mcp_tool`이 내장. mcp 2.2.0은 고수준 `Client(StdioServerParameters)` 제공, 필드는 snake_case(`input_schema`, `is_error`). anthropic의 MCP 브리지 모듈은 파일 단위로 pyright 억제가 걸려 있어 strict에서 Any가 샌다.
- 1라운드 도중 사용자: "LangGraph 쓰고 싶다". 세 가지 뜻(런타임 엔진 / 에이전트를 그래프로 / 선언적 그래프)으로 나눠 물음.
- 의존성 드라이런: `langchain-mcp-adapters` 0.3.2가 mcp<2를 고정해 **mcp 2.2.0 → 1.30.0 다운그레이드**. langchain-anthropic 1.7.2는 anthropic 1.7.0과 호환. 패키지 57 → 87개.
- 결정: **가. LangGraph는 런타임의 엔진.** 플러그인 계약(`run(request, ctx)` → 이벤트 스트림)은 그대로이고 플러그인은 LangGraph를 모른다. 헌법의 "그래프가 아니다"는 유지(코드 플러그인), 의존성 표의 "LangChain 없음"은 ADR로 수정 예정.
- 열린 것: 도입 시점(첫 슬라이스부터 vs 둘째 슬라이스), 체크포인터 범위, ADR 문안, 1라운드 Q2~Q8.

### 나중에 붙을 기능의 확신 표시 (사용자)

> 사용자: "처음 시작할 때 나중에 어떤 기능들을 붙일지 미리 말해 놓아야 더 좋아? 아까 말한 (체크포인트와 되감기, 사람 승인 인터럽트, 그래프 모양의 다단계 에이전트, LangGraph Studio와 LangSmith 시각화. 이 중 인터럽트) 기능들을 생각은 하고 있지만 자연스럽게 추가되는 건지?"
> 사용자: "'기능은 미리 말할 필요 없지만 이음매는 미리 만들어야 한다'라고 했는데 기능을 말해야 이음매를 미리 만들 수 있는거 아니야?"
> 사용자: "나중에 붙이기 어렵다를 내가 판단하기 어려워서 미리 말하려는 건데"
> 사용자: "원격 실행이 높음. 의존성 격리도 포함 하지만 제일 마지막. WASM, 빌더 UI, LangGraph Studio, Slack, 카카오톡만 미정으로 남겨줘."

기능 목록은 약속이 아니라 이음매를 도출하기 위한 입력이다. 여기에만 남기고 헌법에는 이음매(계약 제약)만 적는다.

- **확신 높음**: HTTP API, 웹 채팅 UI, 스케줄러·웹훅 트리거, 사람 승인 인터럽트, 체크포인트·되감기, 토큰 스트리밍, 에이전트가 에이전트 호출, 타임아웃·취소·병렬 도구, 스킬 로더, 핫리로드·레지스트리, **원격·컨테이너 에이전트**, 멀티 프로바이더, 모델 라우팅, 비용 계량, 세션 간 대화 이력, RAG, 트레이스 DB·OTel, 멀티테넌시·주체, PII·가드레일, 헬스체크·Docker.
- **포함하되 제일 마지막**: 플러그인별 의존성 격리(원격 실행과 같은 축).
- **미정**: WASM, 빌더 UI, LangGraph Studio, Slack, 카카오톡.
- 판정: 오늘 설계를 바꾸는 것은 "어려움" 축의 인터럽트(→ 재실행 가능한 run())와 원격 실행(→ AgentContext의 인자·반환은 JSON 직렬화 가능) 둘. 나머지 "중간"은 버전 이음매가 있으면 선택 필드 추가로 끝난다. 멀티테넌시는 선택 필드로 미루면 fail-open이 되므로 `principal` 필수 여부를 따로 묻는다(선행 저장소 D2 교훈).

### 인터뷰 종료와 확정 결정

> 사용자(열린 질문 14개에): "그대로"

사용자가 열린 질문 14개를 전부 추천대로 확정. 프론티어 비움.

- 엔진: LangGraph를 첫 슬라이스부터. 후퇴 조건 2026-09-21. 체크포인터 없음. (ADR 0001)
- 이벤트 주체: 런타임이 run_started·run_failed, 에이전트가 RunFinished 하나. 따라서 `ctx`에 `run_id`와 `now()`가 필요하다(재실행 가능 규칙과 함께).
- 버전 이음매: 매니페스트 `schema_version`, 트레이스 헤더 줄, 모르는 type 보존.
- 제약 둘: `run()` 재실행 가능, `ctx` 메서드 JSON 직렬화 가능.
- `RunStarted.principal` 필수. CLI는 OS 사용자 이름.
- 첫 에이전트 `calc`, MCP `everything`(`npx -y @modelcontextprotocol/server-everything`), 매니페스트 `[server] command/args`, 에이전트 매니페스트 `mcp = ["everything"]`. (ADR 0002)
- 기본 모델 claude-opus-5, `--model`·`AGENT_OS_MODEL`, 폴백 없음, max_tokens 16000.
- run_id uuid4, `traces/<run_id>.jsonl`, `TraceSink` 프로토콜은 core·JSONL 구현은 channel, stdout은 출력·stderr는 `--verbose` 이벤트, 종료 코드 0/1.
- entrypoint는 파일 기준 고유 모듈명(`agent_os_plugins.<name>.<module>`)으로 로드.
- 실패 정책: 서버 시작 실패→run_failed, 도구 에러→ok=false로 모델에 반환, 루프 상한 10, API 에러는 SDK 재시도 뒤 run_failed.
- 산출물: `CONTEXT.md`(용어 17개), `docs/adr/0001`, `docs/adr/0002`, 헌법 v1.1.0.

## 하네스 스코프 정리 (KICKOFF 표 재검토)

> 사용자: "C:\projectgent\KICKOFF.md 의 어디에 무엇이 사는가 내용 다시 한 번 검토해줘. ~/.claude/CLAUDE.md (전역) 이라고 되어있는데... 그리고 전역으로 되어 있는거 전부 프로젝트 스코프로 바꿔죠. (단, 바꿨을 때 문제가 있는지 확인)"

- 공식 문서 확인: 같은 이름 스킬은 enterprise > personal > project 순으로 이긴다(표의 주장 맞음). `skillOverrides: {"이름": "off"}`로 전역 스킬을 프로젝트 설정 파일에서 끌 수 있다. `enabledPlugins`는 객체. 전역 스킬을 끄면 같은 이름의 프로젝트 스킬이 살아나는지는 문서에 없다.
- 실제 전역 상태는 표와 달랐다. grill-me, grilling 외에 git-* 5개, jira-* 5개, 데스크톱 앱이 관리하는 `synced` 폴더.
- 문제 1: 전역 grilling이 프로젝트 grilling(mattpocock)과 동일하지만 전역이 이기므로 `npx skills update` 뒤 낡은 전역이 조용히 이긴다. → 전역 grilling, grill-me를 `~/.claude/skills-backup/`으로 이동. grill-me는 프로젝트 `.claude/skills/`로 복사. 옆 프로젝트 중 이 둘을 참조하는 곳 없음.
- 문제 2: jira-* 5개와 git-pr* 3개는 이 저장소(원격 없음, 로컬 트래커)에서 쓸 일이 없고, ai-agent-platform의 `docs/workflow.md`가 참조하므로 전역에서 빼면 그 프로젝트가 깨진다. → 옮기지 않고 `.claude/settings.json`의 `skillOverrides`로 이 프로젝트에서만 끔. git-commit, git-branch는 범용이라 남김.
- 전역 `CLAUDE.md`는 0바이트라 옮길 것 없음. `synced`는 건드리지 않음.
- pyright-lsp 플러그인을 `--scope project`로 설치(런북 7단계 선택 항목). `.claude/settings.json` 생성.
- 검증: headless 새 세션(`claude -p`)에서 스킬 목록 확인. jira-*, git-pr* 없음. git-commit, git-branch, grilling 있음. grill-me는 `disable-model-invocation`이라 모델 목록에 안 보이는 것이 정상.
- KICKOFF.md 수정 10곳: 표에 Windows 경로, `.claude/settings.json`, `docs/journal/`, `.scratch/` 행 추가. 전역 스킬 원칙을 "프로젝트가 관리하는 스킬의 전역 사본 없음"으로. 0단계 백업 목록에 grilling, grill-me 추가. 2단계에 grill-me 복사 명령. 7단계에 `skillOverrides`. 체크리스트 갱신. 원본 `C:\project\harness-kickoff\KICKOFF.md`는 아직 옛 내용.

### 런북 원본 확정

> 사용자: "커밋해줘. harness-kickoff 원본도 삭제해줘. 이 프로젝트의 KICKOFF.md를 원본으로 할꺼고. 새 프로젝트를 시작 시 이걸로 만들꺼야."

- `C:/project/harness-kickoff/`는 옛 KICKOFF.md 한 파일뿐이었고 이 저장소 첫 커밋의 KICKOFF.md와 동일함을 확인한 뒤 삭제. 내용은 git 이력에 남는다. 파일은 지워졌으나 빈 폴더가 다른 프로세스에 잠겨(Device or resource busy) 남았다. 잠근 프로세스를 닫은 뒤 폴더를 지운다.
- 런북 원본은 이제 `C:/project/agent/KICKOFF.md`. 머리말에 그 사실을 적었다. 새 프로젝트는 이 파일을 복사해 시작한다.

### 관리 화면 요구 추가

> 사용자: "그리고 한가지 빠진게 있어. Agent OS 를 관리할 수 있는 관리 화면이 필요해. 관리 화면에는 Agent 관리, MCP 관리, Model 관리, Skill 관리 등이 필요할 것 같아."

- 확신 표시에서 "빌더 UI"는 미정이었으나, 관리 화면(등록·해제·켜기/끄기·관찰)은 높음으로 올라감. 빌더와는 다른 것으로 구분.
- 판정: 관리 화면은 채널(웹 UI)이며 HTTP 채널의 클라이언트다. 순서는 슬라이스 1 CLI 런타임 → 2 HTTP 채널(목록·실행·트레이스 API) → 3 관리 화면. 선행 저장소가 이 순서를 거꾸로 밟았다.
- 지금 정할 이음매 후보 둘: (1) Model을 네 번째 플러그인 종류로 둘지, (2) 진실의 원천을 파일시스템으로 고정할지(DB 레지스트리 배제). 3라운드 질문으로 발행.

### 관리 화면 3라운드 답과 채널 정의 반박

> 사용자: "1. A / 2. A / 3. A / 4. 그대로. 근데 관리 화면이 채널이 맞아? 채널은 위젯(채팅 화면)만 해당하는거 아니야?"

- 확정: 관리 화면은 관리와 관찰(빌더 아님). Model은 네 번째 플러그인 종류(`plugins/models/`, 첫 슬라이스는 kind만 예약). 진실의 원천은 파일시스템(ADR 0003). 순서는 CLI → HTTP → 관리 화면.
- 산출물: ADR 0003, 용어집에 모델 추가, 헌법 v1.2.0(종류 넷, 진실의 원천, 비목표에 모델 로더). 구현 시 `PluginKind`에 `model` 추가는 티켓으로.
- 사용자 반박이 맞다. 용어집의 채널은 "요청이 들어오고 출력이 나가는 곳", 즉 실행을 일으키는 면이고 관리 화면은 실행을 일으키지 않는다. "관리"라는 개념이 빠져 있었다. 4라운드에서 용어와 패키지 위치를 묻는다.

### 목표 배치와 관리·어댑터 개념 확정

> 사용자: "저장소 구조가 /agent_os { /runtime(또는 core?), /admin{backend,frontend}, /channels{cli, widget{frontend, widget_admin{backend,frontend}}}, /plugins, /docs } 이런식으로 모노레포 구조로 가는건 별로야? (프론트는 NextJS 프레임워크)"
> 사용자: "저 구조가 최선인지 한 번 더 단계적으로 분석하고 검토해줘."
> 사용자: "나중에 충분히 확장 가능한 구조인거지? 만약 같이 작업할 팀원이 생긴다면 작업하기 괜찮은건지도"
> 사용자(확정): "adapters/를 세 번째 형제로 추가하고 JSONL 싱크를 거기 두는 것, 그리고 헌법 거버넌스에 '팀원이 생기면 원격·CI·PR 필수로 전환하고 ADR을 남긴다'"

- 모노레포는 채택. 사용자 제안에서 고친 셋: 파이썬은 한 패키지 유지(분리는 배포가 갈릴 때, sdk는 첫 원격 에이전트에서), `widget_admin`은 채널이 아니라 관리의 화면 하나, 파이썬(`src/`)과 노드(`web/`) 루트 분리(선행 저장소의 글롭 충돌).
- 단계별 재검토에서 고친 둘: 진입점은 조합 모듈(`main.py`, `server.py`)이어야 채널이 관리를 import하는 사고를 막는다. 안쪽 패키지는 `runtime`이 아니라 `core`(용어집의 런타임은 시스템 전체).
- 확장성 검토에서 찾은 구멍: 트레이스 DB·OTel·체크포인터 구현이 살 자리가 없었다. `adapters/`를 채널·관리의 세 번째 형제로 추가. 1라운드 Q5의 "JSONL 싱크는 channel/cli"를 철회하고 adapters로.
- 팀 작업: 구조는 유리(플러그인 작성자는 sdk만, 병렬 작업 지점 분리, 조율 지점은 sdk와 openapi.json 둘). 구조 밖에서 바뀔 것 셋을 헌법 거버넌스에 트리거로 적음: 원격·CI·PR 필수, 이슈 트래커 전환, LLM 테스트는 CI 매 푸시에서 제외.
- 미룬 것: 위젯 기술(Vite 라이브러리 빌드 vs Next.js iframe)은 슬라이스 4에서 실측으로. 관리 화면의 정적 내보내기 서빙은 슬라이스 3에서.
- 산출물: 용어집에 관리·어댑터 추가, 채널 정의 수정. 헌법 v1.3.0(원칙 IV의 의존 방향 확장, 스택 표에 웹, 디렉터리 절 교체, 거버넌스 전환 규칙). 원칙 IV는 규칙이 아니라 열거가 늘어난 것으로 보아 minor로 올렸다. major로 봐야 한다면 2.0.0으로 고친다.
- 구현 시 티켓에 들어갈 것: `main.py`와 스크립트 이동(`agent_os.main:main`), `adapters/trace_jsonl.py`, import-linter 계층 갱신(`main → server → {channel | admin | adapters} → core → sdk`, 없는 층은 선택 표기), `PluginKind`에 `model`, 매니페스트 `schema_version`, `RunStarted.principal`, ctx의 `run_id`·`now()`.

### 헥사고날과 아토믹 디자인

> 사용자: "그리고 헥사고날 아키텍처로 구현할거고. 프론트엔드는 아토믹디자인 패턴을 사용할꺼야."

- 지금 구조는 이미 포트·어댑터 모양(core가 포트, adapters가 구현, channel·admin이 driving 쪽). 정할 것은 정도다. 선행 저장소 ai-agent-platform은 헥사고날을 8개 도메인에 전면 적용(유스케이스 클래스, 커맨드·결과 dataclass, 도메인별 포트)하고 테스트 703개를 세웠지만 LLM 호출 0줄이었다.
- 포트 후보(닫힌 목록): TraceSink, PluginSource(파일시스템 어댑터, ADR 0003), ToolSource(MCP 어댑터), ChatModel(langchain-core 추상), Clock. 결과로 core의 의존성은 langgraph·langchain-core·pydantic, adapters가 langchain-anthropic·langchain-mcp-adapters.
- 아토믹 디자인은 web/apps/admin의 components에 적용, Next의 app/이 pages 역할. 세부 규칙은 슬라이스 3 인터뷰.
- 4라운드 답: 헥사고날은 포트 다섯의 닫힌 목록(A). 의존성 재배치와 import-linter 금지 계약 그대로. 아토믹 디자인 범위 그대로.
- 산출물: 용어집에 포트. 헌법 v1.4.0(포트와 어댑터 절, 의존성 표를 core/adapters로 분리, 프론트 구성 행).
- 구현 시 티켓에 추가: core의 포트 다섯을 Protocol로, `adapters/`에 JSONL 싱크·파일시스템 PluginSource·MCP ToolSource·시스템 Clock, `main.py`에서 ChatAnthropic 조립, import-linter forbidden 계약(core ↛ langchain_anthropic, langchain_mcp_adapters), pyproject 의존성은 한 패키지라 그대로 두되 core에서의 import만 금지.

### 골격을 헌법 배치에 맞춤 (2026-09-20)

> 사용자: "저장소 구조 먼저 바꿔야 하는거 아니야?"

- 맞는 지적. 헌법의 배치가 7단계 스캐폴딩 뒤에 바뀌었으므로, 명세와 티켓이 실제 배치 위에 쓰이도록 첫 슬라이스 범위의 골격을 먼저 맞췄다. `web/`과 `server.py`는 각 슬라이스 몫이라 만들지 않았다.
- 한 일: `src/agent_os/main.py`(조합 층, 스크립트 진입점 `agent_os.main:main`), `channel/cli/main.py`는 파서만 남김, `adapters/`와 `admin/` 패키지 자리(docstring만), `plugins/models/.gitkeep`.
- import-linter 계약 셋: layers `main → (server) → {channel | admin | adapters} → core → sdk`(없는 server는 괄호로 선택), forbidden `core ↛ langchain_anthropic, langchain_mcp_adapters`, forbidden `sdk ↛ langgraph, langchain_core, langchain_anthropic, langchain_mcp_adapters`. 외부 패키지 금지에는 `include_external_packages = true`가 필요했다.
- 검증: ruff, pyright, lint-imports(3 kept), pytest 7 passed, `agent-os --version` 0.1.0.

### 문서 위치 규칙과 병렬 작업 계획 (2026-09-20)

> 사용자: "SSOT 관련해서 최상위 /docs 폴더는 프로젝트의 공통 적인 문서들에 대해서 관리하고 나머지 서비스 별로 /docs를 관리하는 구조를 가져가는건 어떻게 생각해?"
> 사용자: "지금. 그리고 전체 플랜은 최상위에 있고 각 서비스 별 세부 플랜은 따로 가져가는건 어떻게 생각해? 병렬로 작업을 돌리고 싶어."

- SSOT는 폴더가 아니라 사실 단위의 성질. 위치는 범위로 정하고 복사와 요약을 금지하며 루트가 지역 문서를 색인한다. 헌법, 용어집, ADR 번호열은 하나. 헌법 v1.5.0 디렉터리 절에 한 문단으로 추가.
- 병렬 작업 확정: 전체 계획은 `.scratch/plan.md`(기능 목록, Blocked by, Status), 서비스별 세부 계획은 `.scratch/<slug>/`의 spec과 티켓. 프론티어만 병렬. 규칙 셋: 계약 변경 티켓이 첫 blocker, 병렬 브랜치는 rebase 뒤 ff, 티켓 세션의 일지는 파일 분리. 실행 단위는 티켓당 워크트리.
- 산출물: `.scratch/plan.md` 첫 판(슬라이스 1~4, 기능 6개), `docs/agents/issue-tracker.md`에 전체 계획 절, 헌법 v1.5.0(문서 위치, rebase, 병렬 규칙, plan.md).
- 병렬이 값을 내는 시점은 슬라이스 2부터. 슬라이스 1은 사슬이고 원칙 I이 먼저다.

### 선행 저장소 지침 분석과 적용 (2026-09-20)

> 사용자: "@ai-agent-platform/AGENTS.md @frontend/AGENTS.md @backend/AGENTS.md @docs/recurring-cautions.md @docs/workflow.md 이것들 분석해서 처음에 적용할 만한 지침들을 단계적으로 분석하고 검토 후에 적용해줘."

- 다섯 파일의 지침을 넷으로 분류. 이미 있음(모노레포 이유, 용어, ADR 승인, 범위별 문서 위치, TDD, PYTHONUTF8, here-string, 파이프 뒤 &&, 검증 명령 한 곳) / 지금 적용 / "규칙으로" 승인 필요(CODING_STANDARDS) / 슬라이스별 미룸.
- 지금 적용(도구 설정과 한 줄 규칙만, 헌법 v1.6.0): ruff TD 규칙(TD003 제외, `TODO(<슬러그>/<번호>)` 형식), 하네스 변경은 실제 실행으로 확인, 같은 테스트 2회 연속 실패 시 diagnosing-bugs, 훅 시스템은 하나, 커밋 본문은 왜와 남긴 위험, `python` 스토어 스텁 함정, 용어집에 앱(피할 말: 서비스), CLAUDE.md 교정 루프에 3회 문턱.
- 원칙 I과의 긴장: 이것이 LLM 테스트(09-22) 전 마지막 헌법 확장이다. 사용자 요청이고 항목이 한 줄씩이라 넣었다.
- 승인 필요(제안만): CQS, 불리언 플래그 인자 금지, 에러 처리와 정상 흐름 분리, 추상화 수준 맞추기, 출력 인자 금지, 테스트 이름은 한국어 행동 명세, 조건식 추출 기준.
- 미룸: here-string PreToolUse 훅(09-22 뒤 또는 PowerShell 도구를 쓰기 시작할 때), web/CLAUDE.md의 프론트 규칙(앱 간 import 금지, 생성물 편집 금지, Vitest·Playwright 경계, pnpm verify 순서, .env.example)은 슬라이스 3, CodeRabbit·PR 리뷰 분담은 원격 생길 때, 계약 격리 검사는 sdk 분리 때, 새 앱 추가 체크리스트는 슬라이스 2·3, 헌법 분할(원칙만 임포트)은 첫 retro. recurring-cautions 파일은 만들지 않는다. 일지의 함정 항목과 retro가 그 역할이다.

### 선행 저장소의 커스텀 서브에이전트 셋 검토 (2026-09-20)

> 사용자: "@.claude/agents/coderabbit-review.md @plan-reviewer.md @plan-verifier.md 이것도 처음에 만들어 놓으면 좋은지 확인해줘."

- coderabbit-review: 지금 불필요. 같은 일을 하는 플러그인 스킬(`coderabbit:coderabbit-review`)이 이미 user 스코프에 있고, CLI는 청구 주기당 3회라 커밋 게이트로 못 쓴다. 우리 게이트는 `/code-review main`. 선행 저장소의 트리아지 규칙("ruff·pyright·import-linter가 잡는 지적은 오탐")은 나중에 CODING_STANDARDS 한 줄이면 된다.
- plan-verifier: 불필요. mattpocock `code-review`의 Spec 축이 같은 일(구현이 명세와 맞는가)을 한다. 나머지(동결 경로, 아카이브 프론트매터)는 우리에게 없는 계약 동결 훅과 docs/plans에 묶여 있다.
- plan-reviewer: 가치 있음. 구현 전에 명세를 앵커 없이 다시 읽는 검토는 우리 흐름(`/to-spec` → `/to-tickets`)에 없다. 단, 체크리스트를 우리 헌법에 맞춰 다시 써야 하고(alembic, contract-schema, docs/plans 없음), 파일은 아직 만들지 않는다. 첫 슬라이스 명세는 체크리스트를 프롬프트에 넣은 일회성 읽기 전용 서브에이전트로 검토하고, 두 번째 명세에서도 같은 검토를 하게 되면 그때 `.claude/agents/spec-reviewer.md`로 만든다(런북 "반복되기 전에는 도구를 만들지 않는다", 원칙 I).
- 명세 검토 체크리스트 초안(우리 기준): (1) sdk 이벤트·매니페스트 스키마 변경이 계약 영향으로 적히고 schema_version 처리가 있는가 (2) 원칙 I~V 위반이 없는가, 특히 포트 다섯 밖의 새 포트와 core의 프로바이더 import (3) accepted ADR과 충돌하는가 (4) 언급한 경로가 실재하는가 (5) 첫 슬라이스 비목표를 침범하는가 (6) 수용 기준마다 그것을 덮는 테스트가 있고 LLM 테스트가 포함되는가 (7) "적절히 처리" 류 자리표시자가 없는가. 분류는 blocker / should-fix / nit, "blocker 없음"은 정상 결과, 근거 없는 "~일 수 있다" 금지, 읽기 전용.

### 선행 저장소 .claude/rules/backend 14개 분석 (2026-09-20)

> 사용자: "@.claude/rules/backend/{alembic,api,comments,common,common-events,core,domains,domains-adapter-persistence,domains-adapter-web,domains-application,domains-domain,enums,infra-db,tests}.md 이것도 처음에 적용할 만한 지 단계적으로 분석하고 검토 후에 적용해줘."

넷으로 분류했다.

- **의도적으로 채택하지 않음**(헌법 "포트와 어댑터" 절이 이미 거부): domains.md의 도메인별 3계층, domains-application.md의 유스케이스 클래스·Command/Query dataclass·`_shared.py`, common.md의 `common/` 패키지, common-events.md의 이벤트 버스(웹훅이 생기면 재검토). 규칙 자체는 정교하지만 우리가 버린 모양의 규칙이다.
- **아직 없는 것**(DB, FastAPI): alembic.md, infra-db.md, enums.md의 PostgreSQL ENUM, api.md, domains-adapter-web.md, core.md의 ContextVar 주체. 값이 큰 것은 슬라이스 2·DB 씨앗으로 아래에 적는다.
- **지금 적용**(도구 설정과 구조): pytest `--import-mode=importlib`(tests/ 하위 같은 파일명 충돌 제거, 선행 저장소는 훅과 conftest로 막았지만 import 모드 하나로 끝난다. 임시 중복 파일로 실측 확인). `tests/`를 `src/`와 미러링(`tests/sdk/`). 둘 다 헌법을 늘리지 않는다.
- **명세(첫 슬라이스)에 넣을 규약**: (1) Fake와 어댑터는 포트 Protocol을 상속하지 않고 시그니처로 만족한다. 픽스처에서 `sink: TraceSink = FakeTraceSink()`처럼 포트 타입으로 annotate한 줄이 포트 적합성이 검증되는 유일한 지점이다. pyright가 tests를 검사하는 이유. (2) 식별자는 `NewType`(`RunId`, `AgentName` 등). 경계에서 한 번만 감싼다. JSON 형식은 그대로다. (3) 고정 값 집합은 `StrEnum`, 판별자 `type`과 `schema_version`만 `Literal`. `PluginKind`를 StrEnum으로. 근거는 OpenAPI가 이름 있는 `$defs`로 내보내야 슬라이스 3의 생성 클라이언트가 재사용한다는 것. (4) 로깅은 표준 라이브러리. 구조화 기록은 이벤트가 담당하므로 Loguru를 들이지 않는다. (5) async 테스트가 늘면 `pytest-asyncio`(`asyncio_mode = auto`)를 dev 의존성으로.
- **"규칙으로" 후보 추가**(CODING_STANDARDS, 승인 전): 주석은 "왜"만, 죽은 코드는 삭제, 주석 전에 이름 개선. 부재는 `X | None`·실패는 예외로 계층마다 하나. 에러 메시지는 한국어에 식별자 포함. Fake는 포트 상속 금지와 픽스처 포트 타입 annotate. 테스트는 AAA를 빈 줄로 구분. 쓰지 않는 포트를 "언젠가"를 이유로 받지 않는다.
- **슬라이스 2 씨앗**: 예외에 HTTP 상태 코드를 두지 않고 채널의 표 하나가 번역(MRO로 조상 상속). 인증 미들웨어는 fail-open, 보호는 라우트가 opt-in, 그 구멍은 `app.openapi()` 전수 검사 테스트(allowlist 밖 전부 401)로 막는다. 응답 봉투에 request_id. `openapi.json` 내보내기와 "커밋된 스키마가 최신이다" 테스트. 라우터는 조립→호출→변환 세 줄. 응답은 스키마로만, 도메인 객체 직접 반환 금지. 헬스체크는 Depends로 주입.
- **DB 씨앗**(그때 alembic.md를 다시 읽는다): `context.configure()` 전 `connection.execute()`는 조용한 롤백. `alembic_version` 제외 규칙. 드리프트는 `alembic check`. native ENUM은 autogenerate가 못 본다. 통합 테스트는 트랜잭션 롤백으로 격리하고 DB 없으면 CI에서 에러.
- `.claude/rules/` + `paths` 메커니즘: 파일 패턴이 열릴 때만 로드되는 지침 방식. 우리 런북은 중첩 CLAUDE.md를 택했다. 첫 디렉터리 한정 규칙이 생길 때 둘 중 하나로 정한다.

## 추천과 결정이 갈린 곳

- 이름: 추천 `maru`(고유 코드네임), 결정 Agent OS. 사용자는 검색 가능성보다 뜻의 직관성을 택했다.
- 원격 실행 확신: 추정 낮음, 실제 높음. PDF 대화의 "타 언어·타 부서" 맥락을 근거로 낮게 봤는데, 사용자의 관심은 실행 위치 축 자체였다.

## 에이전트가 번복하거나 고친 것

- 1라운드 Q5에서 "JSONL 트레이스 구현은 channel/cli"로 추천했으나, 확장성 검토에서 HTTP 채널이 같은 구현을 쓰게 되는 순간 채널끼리 import하게 됨을 발견해 `adapters/`로 옮겼다.
- 관리 화면을 "채널"로 분류했다가 사용자 지적으로 철회. 채널은 실행을 일으키는 면이고 관리는 구성과 관찰의 면이라 별개 개념이다.
- 옆 프로젝트 조사 결과가 오기 전 Q1을 "새로 시작" 추천으로 냈고, 결과가 온 뒤 이유가 바뀌었다(선행 저장소 둘 다 LLM 호출 0줄). 결론은 같았지만 근거는 달라졌다.
- LangGraph 도입 시점: 처음엔 둘째 슬라이스를 추천했다가, 목표가 엔진으로 정해진 뒤 "루프 100줄을 두 번 쓴다"는 이유로 첫 슬라이스로 번복했다.
- "기능은 미리 말할 필요 없지만 이음매는 미리 만들어야 한다"는 사용자 반박으로 "기능은 말한다, 이음매만 남긴다"로 고쳤다. 난이도 판정은 에이전트의 몫이라는 분업도 이때 정해졌다.

## 다음

- `/to-spec`으로 이 대화를 명세로 만들어 `.scratch/first-slice/spec.md`에 발행. 이어서 `/to-tickets`, `/implement`.
- 원칙 I의 3일 기한: **2026-09-22까지** LLM 호출 테스트 통과. `.env`에 키 필요.
- 10단계 `/retro`.
