# 코딩 표준

code-review 스킬이 리뷰할 때 읽는 파일. 자동 검사(린트, 타입, 테스트, 훅)로 잡을 수 있는 것은 여기 쓰지 않고 검사를 만든다. 아래 판단 기준은 /retro가 제안하고 사용자가 "규칙으로"라고 승인한 것만 남긴다.

## 심각도
Critical(보안, 데이터 유실, 장애) 병합 차단 / Major(명백한 버그, 잘못된 로직) 병합 전 수정 / Minor(가독성, 중복, 누락 테스트) 후속 허용 / Nit 작성자 재량.

## 리뷰하지 않는 것
포맷, 린트, 타입 오류, import 경계, 생성물. ruff, pyright, import-linter, 훅이 잡는다.

## 판단 기준
2026-09-20 승인. 선행 저장소 ai-agent-platform AAPP-47에서 사용자가 고른 목록을 이 스택에 맞춰 옮겼다.

- **불리언 플래그 인자를 만들지 않는다.** `run(request, verbose=True)`는 함수가 두 가지 일을 한다는 신호다. 함수를 쪼갠다.
- **에러 처리와 정상 흐름을 분리한다.** `try` 블록 안에 로직을 쌓지 않고 별도 함수로 뺀다. 실패의 형태는 층마다 하나다. 어댑터는 부재를 `X | None`으로, 실패는 예외로.
- **서드파티는 경계에서 감싼다.** LangGraph, LangChain, anthropic, mcp의 타입이 `sdk`와 채널·관리로 새지 않게 한다. import는 import-linter가 막지만 반환값과 예외로 새는 것은 리뷰가 본다. 경계는 core의 포트다.
- **한 함수 안 문장의 추상화 수준을 맞춘다.** 고수준 호출과 저수준 문자열 조작이 한 함수에 섞이면 쪼갤 신호다.
- **명령과 질의를 섞지 않는다(CQS).** 함수는 무언가를 하거나 무언가에 답하거나 둘 중 하나다. `get`/`is`/`has`/`list`는 질의, `create`/`load`/`emit`/`write`는 명령. `save()`가 저장도 하면서 성공 여부 bool도 돌려주면 호출부가 무시해도 멀쩡해 보이고 실패가 조용히 묻힌다.
- **출력 인자를 쓰지 않는다.** 인자로 받은 객체나 리스트를 변형해 결과로 삼지 않는다. 결과는 새 객체(`dataclasses.replace`, pydantic `model_copy`)나 제너레이터로 돌려준다. 파이썬에는 대응 린트가 없어 여기 적는다.
- **동사 어휘를 통일한다.** 같은 개념에 `fetch`/`get`/`retrieve`/`load`를 섞지 않는다. 동사가 정해지면 `CONTEXT.md`에 동사 표를 두고 그것이 원천이다.
- **테스트 이름은 행동 명세로 쓴다.** 새 테스트 함수명은 한국어 행동 명세다. `test_manifest_parses`가 아니라 `test_agent_매니페스트는_entrypoint가_없으면_거부한다`. 이름에 함수명이나 내부 상태가 들어가면 구현 결합의 신호다. 기존 영문 이름은 손대지 않는다.

## 리뷰 관점
판단이 필요한 것만 넷. 다섯 이상으로 늘리지 않는다.

1. 이 추상화가 실제로 값을 하는가. 재사용도 없고 로직도 단순한데 빼낸 추상화는 비용만 남는다. 포트 다섯 밖의 새 추상화는 특히.
2. 이름이 `CONTEXT.md`의 용어를 반영하는가. 피할 말 목록에 있는 낱말이 코드에 있으면 지적한다.
3. 이 모듈이 하나의 이유로만 바뀌는가. 보조 질문: 어떤 함수가 자기 데이터보다 다른 객체의 데이터를 더 많이 쓰고 있지 않은가.
4. 테스트가 행동을 검증하는가, 구현을 복제하는가.

보안·버그·성능은 PR 직전 CodeRabbit CLI와 PR의 CodeRabbit이, 이 넷과 경계는 PR의 Claude Code Review가 본다. 누가 언제 보는지는 `docs/constitution/operations.md`의 리뷰 파이프라인.
