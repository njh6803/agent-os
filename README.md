# Agent OS

플러그인 기반 에이전트 런타임. an agent OS.

규칙과 구조는 [docs/constitution.md](docs/constitution.md), 진행 기록은 [docs/journal/](docs/journal/).

```bash
uv sync
uv run pre-commit install
uv run pytest -q
```
