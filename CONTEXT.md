# Agent OS

플러그인 기반 에이전트 런타임의 용어집. 정의는 "무엇인가"만 적고 구현은 적지 않는다. 결정은 `docs/adr/`에 있다.

## 구조

**Agent OS**:
이 프로젝트. 에이전트, MCP 서버, 스킬을 플러그인으로 로드해 실행하는 런타임.
_Avoid_: Agentic OS(태그라인에서만), 플랫폼

**런타임(Runtime)**:
플러그인을 로드하고 실행을 구동하는 것.
_Avoid_: 엔진, 호스트, 커널

**플러그인(Plugin)**:
런타임이 지정 디렉터리에서 로드하는 단위. 종류는 agent, mcp, skill, model 넷.
_Avoid_: 확장, 모듈, 봇

**에이전트(Agent)**:
개발자가 코드로 쓰는 플러그인. 요청 하나를 받아 이벤트를 내며 출력을 만든다.
_Avoid_: 봇, 워크플로우, 그래프

**채널(Channel)**:
런타임에 실행을 요청하고 출력을 받는 면. CLI가 첫 번째. 실행을 일으키지 않는 것은 채널이 아니다.
_Avoid_: 트랜스포트, 인터페이스, 관리 화면

**관리(Admin)**:
런타임의 구성(플러그인)을 읽고 바꾸며 실행을 관찰하는 면. 실행을 일으키지 않는다.
_Avoid_: 제어면, 컨트롤 플레인, 콘솔, 대시보드, 관리 화면(화면은 관리의 웹 UI일 뿐)

**어댑터(Adapter)**:
core가 정의한 포트의 구현. 파일, DB, 외부 서비스에 닿는 코드. 실행을 일으키지도 구성을 바꾸지도 않는다.
_Avoid_: 인프라, 스토어, 드라이버

**포트(Port)**:
core가 프로세스 밖과 닿는 지점마다 하나씩 선언한 인터페이스. 목록은 헌법에 있고 닫혀 있다.
_Avoid_: 게이트웨이, 서비스 인터페이스, 리포지토리

**매니페스트(Manifest)**:
플러그인의 종류, 이름, 버전, 진입점을 적은 `plugin.toml`.
_Avoid_: 설정, 프로필

**도구(Tool)**:
모델이 호출하는 함수. MCP 서버가 제공한다.
_Avoid_: 스킬, 액션, 함수

**MCP 서버(MCP Server)**:
도구를 제공하는 외부 프로세스. mcp 종류 플러그인이 가리킨다.
_Avoid_: 툴 서버

**스킬(Skill)**:
에이전트의 컨텍스트에 주입되는 절차 지식의 폴더. 도구가 아니다.
_Avoid_: 툴, 플러그인(일반 지칭)

**모델(Model)**:
이름을 가진 LLM 설정. provider, model id, 기본 파라미터를 묶는다. API 키 값은 담지 않는다.
_Avoid_: 프로바이더 설정, 엔드포인트, 모델 프로필

## 실행

**실행(Run)**:
에이전트가 요청 하나를 처리하는 한 번. run_id로 식별한다.
_Avoid_: 세션, 작업, 잡, 스레드

**요청(Request)**:
실행의 입력 문자열.
_Avoid_: 프롬프트, 메시지, 질문

**출력(Output)**:
실행의 결과 문자열.
_Avoid_: 답변, 응답, 결과

**주체(Principal)**:
실행을 요청한 사람이나 시스템의 식별자. 모든 실행에 있다.
_Avoid_: 사용자, 테넌트, 소유자

**이벤트(Event)**:
실행 중 일어난 일의 기록 단위. run_id와 시각을 가진다.
_Avoid_: 로그, 메시지

**트레이스(Trace)**:
한 실행의 이벤트를 순서대로 저장한 것.
_Avoid_: 로그, 히스토리

**루프(Loop)**:
모델 호출과 도구 실행을 모델이 멈출 때까지 반복하는 것. 런타임이 소유한다.
_Avoid_: 에이전틱 루프, ReAct, 에이전트 루프

**컨텍스트(Context)**:
런타임이 에이전트에게 넘기는 손잡이. 에이전트는 이것으로만 모델과 도구를 쓴다.
_Avoid_: 클라이언트, 서비스, 환경
