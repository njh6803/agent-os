# Agent OS

플러그인 기반 에이전트 런타임의 용어집. 정의는 "무엇인가"만 적고 구현은 적지 않는다. 결정은 `docs/adr/`에 있다.

## 구조

**Agent OS**:
이 프로젝트. 에이전트, MCP 서버, 스킬을 플러그인으로 로드해 실행하는 런타임.
_Avoid_: Agentic OS(태그라인에서만), 플랫폼

**런타임(Runtime)**:
플러그인을 로드하고 실행을 구동하는 것.
_Avoid_: 엔진, 호스트, 커널

**앱(App)**:
따로 배포되는 단위. 파이썬 런타임, 관리 화면, 위젯.
_Avoid_: 서비스, 프로세스

**플러그인(Plugin)**:
런타임이 지정 디렉터리에서 로드하는 단위. 종류는 agent, mcp, skill, model 넷.
_Avoid_: 확장, 모듈, 봇

**에이전트(Agent)**:
개발자가 코드로 쓰는 플러그인. 요청 하나를 받아 이벤트를 내며 출력을 만든다.
_Avoid_: 봇, 워크플로우, 그래프

**채널(Channel)**:
런타임에 실행을 요청하고 출력을 받는 면. CLI가 첫 번째. 실행을 일으키지 않는 것은 채널이 아니다.
_Avoid_: 트랜스포트, 인터페이스, 관리 화면

**관리(Admin)**:
런타임의 구성(플러그인)을 읽고 바꾸며 실행을 관찰하는 면. 실행을 일으키지 않는다.
_Avoid_: 제어면, 컨트롤 플레인, 콘솔, 대시보드, 관리 화면(화면은 관리의 웹 UI일 뿐)

**어댑터(Adapter)**:
core가 정의한 포트의 구현. 파일, DB, 외부 서비스에 닿는 코드. 스스로 실행을 일으키거나 구성을 바꾸지 않고, 채널과 관리가 정한 것을 바깥에 옮긴다.
_Avoid_: 인프라, 스토어, 드라이버

**포트(Port)**:
core가 프로세스 밖과 닿는 지점마다 하나씩 선언한 인터페이스. 목록은 헌법에 있고 닫혀 있다.
_Avoid_: 게이트웨이, 서비스 인터페이스, 리포지토리

**매니페스트(Manifest)**:
플러그인의 종류, 이름, 버전, 진입점을 적은 `plugin.toml`.
_Avoid_: 설정, 프로필

**표지(Placeholder)**:
목록에서 읽히지 않는 항목의 자리에 대신 서는 항목. 조용히 빼지도 않고 목록 전체를 실패시키지도 않는다. 아는 것과 읽을 수 없는 이유를 든다.
_Avoid_: 에러 항목, 스텁, 툼스톤, 널 오브젝트

**읽을 수 없는 매니페스트(Unreadable Manifest)**:
매니페스트 목록에서 매니페스트로 읽히지 않는 것의 자리에 서는 표지. 종류와 이름과 이유를 든다. 매니페스트가 아니다.
_Avoid_: 깨진 플러그인, 잘못된 매니페스트, 매니페스트 에러

**꺼진 플러그인(Disabled Plugin)**:
제자리에 있지만 운영자가 꺼서 런타임이 쓰지 않는 플러그인. 지워진 것이 아니고, 끈 것은 플러그인이 스스로를 적은 내용이 아니라 운영자의 결정이다.
_Avoid_: 비활성 플러그인, 등록 해제, 차단, 중지, 일시정지(실행의 상태)

**도구(Tool)**:
모델이 호출하는 함수. MCP 서버가 제공한다.
_Avoid_: 스킬, 액션, 함수

**MCP 서버(MCP Server)**:
도구를 제공하는 외부 프로세스. mcp 종류 플러그인이 가리킨다.
_Avoid_: 툴 서버

**스킬(Skill)**:
에이전트의 컨텍스트에 주입되는 절차 지식의 폴더. 도구가 아니다.
_Avoid_: 툴, 플러그인(일반 지칭)

**모델(Model)**:
이름을 가진 LLM 설정. provider, model id, 기본 파라미터를 묶는다. API 키 값은 담지 않는다.
_Avoid_: 프로바이더 설정, 엔드포인트, 모델 프로필

## 실행

**실행(Run)**:
에이전트가 요청 하나를 처리하는 한 번. run_id로 식별한다.
_Avoid_: 세션, 작업, 잡, 스레드

**요청(Request)**:
실행의 입력 문자열.
_Avoid_: 프롬프트, 메시지, 질문

**출력(Output)**:
실행의 결과 문자열.
_Avoid_: 답변, 응답, 결과

**주체(Principal)**:
실행을 요청한 사람이나 시스템의 식별자. 모든 실행에 있다.
_Avoid_: 사용자, 테넌트, 소유자

**이벤트(Event)**:
실행 중 일어난 일의 기록 단위. run_id와 시각을 가진다.
_Avoid_: 로그, 메시지

**트레이스(Trace)**:
한 실행의 이벤트를 순서대로 저장한 것.
_Avoid_: 로그, 히스토리

**이벤트 스트림(Event Stream)**:
한 실행의 이벤트를 생기는 대로 채널이 내보내는 열이고, 저장된 쪽이 트레이스다. 단위는 이벤트이고 모델의 토큰이 아니다.
_Avoid_: 토큰 스트림, 로그 스트림, 피드

**실행 상태(Run Status)**:
한 실행이 지금 어디에 있는지. 트레이스에서 파생되는 값 넷이다. 일시정지, 끝남, 실패, 그리고 결말 없음 — 마지막은 "지금 돌고 있다"가 아니라 끝내지 못하고 사라진 실행과 구분되지 않는다는 뜻이다.
_Avoid_: 진행 중, 실행 중, 대기

**실행 요약(Run Summary)**:
목록에서 보이는 한 실행의 개요. 트레이스 전체가 아니다.
_Avoid_: 런 메타, 실행 정보, 트레이스 항목

**읽을 수 없는 트레이스(Unreadable Trace)**:
실행 목록에서 읽히지 않는 트레이스의 자리에 서는 표지. 실행 식별자와 이유를 든다. 실행 요약이 아니다.
_Avoid_: 깨진 실행, 손상된 트레이스, 부분 요약

**루프(Loop)**:
모델 호출과 도구 실행을 모델이 멈출 때까지 반복하는 것. 런타임이 소유한다.
_Avoid_: 에이전틱 루프, ReAct, 에이전트 루프

**컨텍스트(Context)**:
런타임이 에이전트에게 넘기는 손잡이. 에이전트는 이것으로만 모델과 도구를 쓴다.
_Avoid_: 클라이언트, 서비스, 환경

**프롬프트(Prompt)**:
에이전트가 요청을 가공해 컨텍스트의 `llm()`에 넘기는 문자열. 요청이 아니다.
_Avoid_: 요청(둘을 섞지 않는다), 메시지

**모델 응답(Model Reply)**:
루프 안에서 모델을 한 번 부른 것이 돌려준 텍스트와 토큰 수와 도구 호출. 출력이 아니다.
_Avoid_: 출력(실행 단위의 말), 답변, 메시지

**도구 호출(Tool Call)**:
모델이 요청한 도구 하나와 그 인자. 아직 실행되기 전이다. 실행된 기록은 `tool_called` 이벤트다.
_Avoid_: 함수 호출, 툴콜, 액션

**도구 결과(Tool Result)**:
도구 하나를 부른 것이 돌려준 성공 여부와 내용. 실패면 내용이 에러 내용이다.
_Avoid_: 출력, 응답

**도구 연결(Tool Connection)**:
실행 하나가 지정한 MCP 서버들에 붙어 있는 동안의 상태. 실행이 끝나면 닫힌다.
_Avoid_: 세션, 클라이언트

## 인터럽트

**인터럽트(Interrupt)**:
실행이 바깥의 결정을 기다리며 스스로 멈추는 일. 승인이 그 첫 종류다.
_Avoid_: 중단(실행을 끝내는 것), 브레이크포인트, 예외

**일시정지(Paused)**:
인터럽트로 멈춘 실행의 상태. 실패도 아니고 끝난 것도 아니다.
_Avoid_: 대기, 보류, 중단

**승인(Approval)**:
사람이 도구 호출 하나를 허가하거나 거부하는 것. 거부도 승인의 결과 중 하나다.
_Avoid_: 확인, 허가, 컨펌, 검토

**결정(Decision)**:
승인 하나가 내린 답. 허가이거나, 사유를 붙인 거부다.
_Avoid_: 판정, 응답, 승인 결과

**승인자(Approver)**:
승인 결정을 내린 주체. 실행을 요청한 주체와 같을 수 있다.
_Avoid_: 검토자, 관리자, 결재자

**재개(Resume)**:
일시정지한 실행을 같은 실행 그대로 이어서 끝내는 것. 새 실행이 아니다.
_Avoid_: 재시작, 복구, 재실행(처음부터 다시 하는 것)

**재생(Replay)**:
재개할 때 이미 한 모델 호출과 도구 호출을 다시 실행하지 않고 트레이스에 기록된 값으로 대신하는 것.
_Avoid_: 리플레이, 재실행, 캐시
